# UpdateState — Conversation History

Short summaries of what was discussed or changed in each session.
Most recent entry first.

---

**2026-09-23** — GitHub Pages-deploy zonder eigen domein toegevoegd voor `/rekenraak/`: aparte Vite-base, werkende statische links en media, Pages-URL's in metadata/sitemap en een Actions-workflow op `main`. Pages-build, normale build, browserpreview en `npm run check` (1275 tests) gecontroleerd.

**2026-09-23** — De voettekst kan "Gemaakt met RekenRaak.be" nu verbergen via "Niet tonen"; alle drie de voettekstposities zijn dan beschikbaar. Oudere werkbladen blijven het merk links tonen. Delen via link getest; `npm run check` groen (1275 tests) en de bladweergave in de browser gecontroleerd.

**2026-09-20** — Paginamarges verbreed naar 20mm rondom (was 8mm boven/onder, 14mm zijkanten) — op eigen verzoek "maak ze normaal voor afgedrukte documenten". `index.css` (scherm-px én print-mm padding op `.page-sheet-head/-body/-foot` en de `.page-sheet-warn`/`.page-tail-hint` insets), `FULL_BLOCK_WIDTH_PX` (BlockWidthContext.tsx) 688→642px, `BODY_HEIGHT_PX`-formule in `blockLayout.ts` bijgewerkt (header-chrome 96→142px, footer-chrome 71→117px, dus `ROW_BUDGET` 37→34 en `PAGE_BODY_PX` 916→824). De smallere kolommen verschoven de kwart/half-kolombreedte (151→139px, 330→307px); `ordenenFloor`'s hardgecodeerde `330-28`-drempel is nu `tierWidthPx(2) - COL_GAP_PX` (dynamisch). `scripts/width-matrix.mjs` opnieuw gedraaid tegen de dev-server; de `LAYOUT`-tabel in `blockLayout.ts` bijgesteld voor de typeIds die daadwerkelijk van kolom veranderden (`hr-std-optellen/aftrekken/delen/gemengd` passen niet langer op een kwart, `mab-herkennen`/`mab-tekenen` niet langer 2-up op vol, `vormleer-punt-lijn` iets hoger). Testfixtures in `blockLayout.test.ts`, `viewers.smoke.test.tsx`, `viewers.stale.test.tsx` bijgewerkt naar de nieuwe pixelwaarden. Visuele baseline volledig geregenereerd (`npm run visual:baseline -- --all`, 76 van de 768 rijen gewijzigd). ARCHITECTURE.md (paginabudget-formule, print-geometrie SYNC-notitie) en CLAUDE.md (print-geometrielijn) gesynchroniseerd. Gate groen: 1274/1274 tests.

**2026-09-14 (7)** — Fable + drie Opus-agents op `dev`. **Voettekst-kader:** de tekst raakte op papier de rand — de print-regel `.print-tfoot-inner { padding … !important }` overschreef de inline kader-padding (en `footerCustom.padX/padY`); regel herschaald naar `.page-sheet-foot .print-tfoot-inner` zonder `!important` op padding, kader op `8px 12px`, scherm = print gecontroleerd. **Nummering per oefening** voor hoofdrekenen (`hr-std-*`) en rekenvolgorde: Opmaak › Nummering Geen / `1)` / `a)` (`MathBlock.itemNumbering`, top-level, presentatie: geen stale-vlag via `PRESENTATION_SAFE`, door de curriculum-lock, geen format-bump); vaste labelkolom per blok zodat `=` op één x blijft; Kort blijft 2-up (label niet aangerekend op de Kort-schatting, wel op Stappen). **README:** live, KlasCement in tekst, documentatietabel, ~60 registry-types; `package.json` 1.0.0 + homepage/repository. **Commit-gate:** `.githooks/pre-commit` (via `npm run prepare` → `core.hooksPath`) draait `npm run check` en `scripts/visual-gate.mjs --staged` (staged bestanden → typeIds, gedeeld oppervlak → alle bladen; 768 rijen in 82 s tegen `scripts/visual-baseline.json`, `npm run visual:baseline` om te accepteren); `font-baseline`/`font-compare` delen nu `scripts/lib/`. Omzeilen (`--no-verify`, `SKIP_GATE`, `SKIP_VISUAL`, `core.hooksPath`) wordt door de PreToolUse-hook `commit-bypass-guard.ps1` een permissieprompt die alleen een mens beantwoordt. BUGS: breedtekiezer zet het blok op 'verouderd'.

**2026-09-14 (6)** — Ruben's round-4 feedback op vormleer: punt-lijn herkennen én tekenen lezen nu uit één scenario-bouwer per niveau (`buildScenario`: 1 = één benoemd element, 2 = twee elementen in één benoemde relatie, 3 = keten van drie stappen); elk punt/rechte/lijnstuk draagt altijd een naam; nieuwe pills Horizontaal/Verticaal (`allowHorizontaal`/`allowVerticaal`); hoeken tekenen kreeg "Hoeken benoemen" (`nameAngles`, standaard aan, "Teken: rechte hoek ABC"). `VormleerExercise` kreeg `elements`/`steps`; `subExercises` alleen nog voor oude bladen. CI naar node 24 (deprecatiemails). Tweede feedbackronde: labels botsingsvrij geplaatst (`scenarioLayout.ts`, 200 seeds getest), circumflex op het hoekpunt (AB̂C), "Figuren per rij" weg voor punt-lijn. Lege-bladtekst is nu een leerkrachtgids in zes stappen; Blad-subtabs in gelijke derden; eenmalige "Marges op Geen"-modal vóór de eerste print (`PrintHintModal`, ook bij Ctrl+P). Catalogus opnieuw gegenereerd.

**2026-09-14 (5)** — Release-batch ronde 4 op `dev` (Fable + 2 Opus + 3 Sonnet-agents, disjuncte bestandssets, 19 commits): **store/persistentie** — sjabloon-export wist élk registry-oefeningveld (`stripBlock` uit `REGISTRY`), mislukte autosave toont een rode stip + "Kon niet bewaren" (`saveState: 'error'`), Genereer alles is één undo-stap, count-verhoging loopt via `generateExtra` (zelfde top-up/dedupe/notitie als eerste generatie); **meting/layout** — optimistische breedtekiezer (`pickerMinWidthUnits`: ¼ direct vanaf 1/1, packer klemt terug), even-oneven rooster geeft `shrinks` door (intrinsieke breedte stabiel over runs), plaatswaarde-omcirkelen pijlen in `ch` uitgelijnd, geld tekenen/wissel veto ½; **vormleer** — niveau 1/2/3 ook voor punt-lijn tekenen, geen kader rond hoeken meten, eigenschappentabel past zich aan de blokbreedte aan (stapelt kolommen); **breuken** lijnstuk-rekenrij wrapt op ¼; **tests/CI** — pairwise-matrix bewijst dekking, `strict: true`, `.github/workflows/check.yml`; **onboarding** — eerste bezoek toont `WelcomeModal` (rondleiding / demo-video / overslaan), Help kreeg "Bekijk de video"; catalogus-script CRLF-fix + regen; klascement.md kort en verwijst naar de site. BUGS.md leeg op de docs-regel na. Gate groen (1217 tests), matrix 0 regressies. Screenshots: `Downloads/review4-check/`.

**2026-09-14 (4)** — Statische pagina's op de échte app-CSS: `about.html`, `faq.html`, `oefeningen.html` staan in de projectroot als extra Vite-pagina's (`src/site.ts` importeert `index.css`), met de app-klassen: tabs Over · Vragen · Oefeningen in de sidebar-kopstrook (seg-group), woordmerk onderaan vastgepind, topbar enkel "Open RekenRaak", geen footer; scroll-spy kiest de sectie aan de bovenrand. Inhoud herschreven: about (bouwmodel in drie kolommen, manifest vooraan, Voor wie derde, Hoe werkt het in vier stappen incl. curriculum en OneDrive-tip, KlasCement in woorden, auteur = RareGoudvis), faq (afdrukknop, werkblad vs curriculum, tablet geblokkeerd, roadmap september 2027, feedbackformulier rechtstreeks). Catalogus terug op een eigen pagina, kaarten met `data-leaf`, witte schermafbeeldingen (blok gedeselecteerd). **Nooit meer stale:** `catalogue.test.ts` laat `npm run check` falen als `oefeningen.html` niet dezelfde bladen draagt als `APP_STRUCTURE`, de Stop-hook waarschuwt bij een appstructure-wijziging zonder catalogus, CLAUDE.md stap 8. `REVIEW-CHECKLIST.md` weg — de reviewlijst is `.claude/docs/REVIEW.local.md` (gitignored). Breedtematrix herdraaid: geen regressies.

**2026-09-14 (3)** — "Over dit project"-modal weg: sidebar-woordmerk en topbar-menu linken naar `/about.html`. De statische pagina's (`about.html`, `faq.html`) staan op een replica van de app-chrome (`public/site.css`): topbar = navigatie Over / Vragen & oefeningen + "Open RekenRaak", sidebar = ankers (over) of vragen + oefeningen per domein met kleurrails (faq; klik op een oefening toont enkel die kaart, "Toon alle oefeningen" zet alles terug), geen inspector; `oefeningen.html` is opgegaan in `faq.html` (`npm run catalogue` splitst de kaarten tussen markeer-commentaren in). Breedtematrix herdraaid na ronde 3: geen regressies, acht cellen passen nu (geld tekenen/wissel ¼, verbanden/geld-rekenen/tijdsduur ½).

**2026-09-14 (2)** — Reviewronde 3, batches 2–6 op `fix/r3-bewerkingen-meten` (Fable + max. 2 Sonnet-agents). **Bewerkingen:** cijferen standaard 4 oefeningen, 4/2/1 per rij per breedte (decimaal × 3, decimaal : 2; `LAYOUT perRowFull` 4); tafels-pillen laten ruimte; controleren-controlelijn op de schrijfruimte-token. **Meten:** tijdsduur en korting/intrest/winst kolommen in `ch` uit de breedste waarde → passen op ½ (`minWidth` 2); bedrag tekenen 3-up vol / 2-up ½ via `fitCols` (veto weg), wissel 1-up op ½; lege digitale klok 84×40; oppervlakte/omtrek/lengte elke figuur eigen SVG-hoogte (geen gat onder kleine figuren); **weegschaal "Wijzer tekenen" → "Weegschaal kleuren"** (geen wijzer, leerling kleurt de boog tot de massa, oplossing = rode taartpunt; oude `tekenen` laadt nog); maateenheid zin links + eenheidchips op eigen ingesprongen regel, geen ¼; herleidingen enkel vol. **Geen dubbele oefeningen (Blad › Opdrachten, standaard aan):** `generateForBlock()` in `generateDispatch.ts` ontdubbelt op inhoudssleutel (`exerciseKey` op de registry-rij of de oefening zonder ids), rolt tot 8× bij, vult een te kleine reeks aan met herhalingen + notitie; alle paden (Genereer, alles, eerste toevoeging, moduswissel, aantal verhogen). **Vormleer:** punt/lijn/rechte herkennen met `niveau` 1–3 (benoemde elementen, relatiezinnen loodrecht/evenwijdig/snijdt in punt/ligt op, twee relaties), nieuw blad Hoeken › Meten (hoek op schaal, 5°-stappen, hulplijn 0–180°), beide vloer ½. **SEO:** `public/faq.html` (16 vragen, FAQPage JSON-LD) en gegenereerd `public/oefeningen.html` + 128 png's via `npm run catalogue` (TESTING.md), gelinkt vanuit about/modal/sitemap. **Voor Ruben:** `REVIEW.local.md`-rijen op scherm + print nakijken; niveau-3-layout (twee figuren boven twee zinnen) en de kleurraster-png (107 KB) zijn oordeelpunten; `npm run matrix` nog niet herdraaid.

**2026-09-14 (1)** — Reviewronde 3, batch 1 (Getallenkennis) op Rubens "→"-opmerkingen in `REVIEW.local.md`; Fable superviseert, twee Sonnet-agents op één branch. **Breedteklem (1a):** `minWidthUnits` beoordeelt nu álle metingen van een blok (overloop = eis, passend-en-herschikkend = één tier smaller toegestaan) i.p.v. enkel de breedste, en `FragmentableGrid` meldt `data-cols`/`data-shrinks` aan de probe — MAB tekenen, verliefde harten, splitsbenen ≤100, tafels (×, :) en temperatuur bereiken ¼ (BUGS-regel "fitCols-viewer bereikt zijn tier nooit" weg). **Uitlijning (1b):** splitsen rooster 4/2/1 naast elkaar zonder rek, splitsboom wikkelt i.p.v. banner, plaatswaarden "=" op één x, vergelijken-kiezen op de komma uitgelijnd, plaats-omcirkelen pijlen op één x, MAB-stijlkaarten zo breed als de keuzelijst. **Centreren (1c):** `centerWhenSingle()` voor ordenen/rangschikken/procenten/romeinse/deelbaarheidstabel/kleurraster op ½; deelbaarheid-omcirkelen wikkelt; verbanden-tabel kolommen op de breedste waarde (`ch`) → past op ½ (`minWidth` 2). Volgende batches: Bewerkingen, Meten, geen-dubbele-oefeningen, vormleer (benoemen met relaties + hoeken meten), faq/oefeningen-pagina's.

**2026-09-13 (7)** — Reviewronde 2 (§0-fixqueue, §1 Getallenkennis, §2 Bewerkingen uit `REVIEW.local.md` + standaardopdracht per blad), Fable superviseert, agents per git-branch in eigen worktree. **Gelandt:** `BlockErrorBoundary` rond elke Viewer (blad, miniatuur, voorbeeldkaart) — een crashende viewer houdt zijn titelrij en toont "Kon dit blok niet tekenen — klik Genereer" (`.no-print`), reset op de oefeningen-array; 64 tests (elke viewer met misvormde data). ESLint negeert `.claude/worktrees`. **Eigen data (B/H):** de stale-sweep (`viewers.stale.test.tsx`, elk blad × elke optie, 338px) vond geen crashers; weegschaal en klok dragen nu hun eigen bereik/notatie resp. modus/kloktype op de oefening; een modus-wissel in breuken/MAB regenereert i.p.v. te wissen; viewer-README regel 6. **Reviewbugs (E):** kalender maandrooster op ½ (tekst wikkelt, `minWidth` terug 2), vormleer-mini past in zijn cel, **horizontale overloopbanner** per cel ("N px te breed" + Verklein/Verbreed), ¼-pas voor de zinstypes (al veilig), maateenheid + kalender gecentreerd op ½. **Getallenkennis (C1, 13 commits):** `SETTINGS_FLOOR` (instellingsafhankelijke editoriale breedtevloer naast `VETO_MIN`); patronen zonder verticale ketting (¼ weg); getallenas/-rijen/functie enkel vol, lettertype volgt de schuif, functie eigen subdomein; procenten 1-up op ½; veelvouden reeks op breedte gecapt; deelbaarheidstabel zonder "deelbaar door"; deelbaarheid-kleuren cellen in em, laatste rij aangevuld, kleurraster = rooster-vorm `rasterVorm`; even-oneven op ½; splitsen kolommen per layout + maxGetal, `mathOrder` gehusseld; MAB stijl inline kaarten, tekenen op ¼; plaatswaarde "omcirkelen" (nieuw blad); breuk-lijnstuk gecapt op halve kolom, hoeveelheid-rekenrijen uitgelijnd; afronden koppen "T"/"H". **Over-pagina (F):** `public/about.html` volwaardig (SEO-meta, JSON-LD, domeinen + aantallen), modal ingekort. **Bewerkingen (C2):** delen-met-rest op de termvakken van het blok; tafels op ¼; breuktermen krijgen één termbreedte per blok (`=` op één x); cijferen-ruitjes volgen de lettergrootte (`cijferGrid.ts`), sluitlijn niet meer afgeknipt, ≤3 naast elkaar gemeten uit de oefeningen, `decimalPlaces` op de oefening; negenproef-kruisen op één kolom, controlelijn schrijfbaar; schattend één rasterkolomstel per rij + `answerLine` kort/lang; breuken-bewerken kolommen uit de breedte (¼ mogelijk). **Ordenen (C3):** één CSS-grid per oefening (antwoordvakjes exact onder de getallen), nooit wikkelen (½ enkel als de rij op één regel past — `ordenenRowPx` in `SETTINGS_FLOOR` én de viewer), `answerStyle` lijn/vak; vergelijken-kiezen standaard 3 getallen (min 2). **Standaardopdracht per blad (D):** `LeafExercise.instruction` (tekst of functie van de instellingen), `MathBlock.leafId`, `addBlockFromType(…, opts)`, één `resolveInstruction()` voor blad, sjablonen en curriculum; alle ~120 bladen gevuld (pupil-facing, Vlaams), `instructions.test.ts`. **Schrijfruimte (G):** één token `--sheet-answer-h` (18px bij 13pt, volgt de cijferschuif) met schuif Blad › Opdrachten › Schrijfruimte (14–32) en per-blok-override; alle antwoordlijnen als factor ervan (`ANSWER_LINE_H` / `ANSWER_ROW_H`), regressiewacht: 0 verschillen bij standaard voor de 18/32px-sites, de 15–16/20–22px-uitschieters in één gereviewde commit genormaliseerd (58 bladen, rapport in de commit), hoogte-audit groen op 18 en 28; boxen met eigen schuif ongemoeid. README vernieuwd. **Skyline-packing (I):** de packer houdt per pagina een skyline per kolomeenheid en zet een blok op de laagste plek waar het past, dus een ½-blok schuift onder een korter ½-buurblok (schakelaar Blad › Opdrachten › "Blokken aansluiten"; `rijen` = oude rijenlayout, byte-gelijk getest); cellen absoluut gepositioneerd in `.page-sheet-canvas`; hoogte-audit (a2) toont geen gerekte cellen meer. **Opruiming (J):** `public/icons.svg` en het 1000px-matrixbestand weg, §11 klopt weer met de boom. **Voor Ruben:** `REVIEW.local.md` (✅-rijen nakijken op scherm + print), plus de I-leesvolgorde en de G-genormaliseerde bladen (verbanden-tabel −50px, procenten/herleidingen +10–12px).

**2026-09-13 (6)** — Vervolg op de reviewronde, na Rubens blik op het blad. **Nooit stil verkleinen:** ScaledBlock paste een blok tot 85 % in zijn kolom (een ¼-blok naast een ½-blok met dezelfde instellingen was kleiner); nu rendert elk blok op de gevraagde zoom en wordt te brede inhoud verbreed (`promoted`) — verkleinen is een expliciete schakelaar per blok (Opmaak › "Verklein om in de kolom te passen", badge "verkleind tot N %", vloer 85 %, tier beoordeeld op px×0,85). Onderliggende oorzaak dat ¼ toch toegelaten werd: `intrinsicOf` nam de smalste meting ooit, en een blok wordt ook vóór Genereer gemeten (67 px lege-staat-regel) — metingen vervallen nu bij gewijzigde inhoud en de breedste tier wint. Breedtematrix-regel: zoom 1 (numeriek ongewijzigd). Rekenvolgorde: cijfers stonden op 0,92× de token t.o.v. 1× bij hoofdrekenen (geen glyph-fallback, gemeten) → 1×; Kort/Lang/Stappen-antwoordlijnen via `RekenvolgordeStyleConfig`; ½ nu toegelaten. Vergelijken: 1-up-rijen gecentreerd, ¼-antwoordvak verdween door flex-krimp (`flexShrink: 0`). Onthoudkader-tekst op bladgrootte (titel 1×, tekst 0,85× tekst-token). Nummeringsbug: opdrachttekst opnieuw aan wist `skipNumbering`; een overgeslagen blok toont nooit "0.". Survey voor de leaf-review, links-zwaar op ½: maateenheid, getalfunctie (schrijven), kalender (datum-rekenen), getallenrijen. BUGS: horizontale overloop op volle breedte knipt zonder banner.

**2026-09-13 (5)** — Reviewronde §0/§5, batch 3 (twee Opus-agents): de SVG-figuren volgen nu de Lettergrootte-schuiven — klokwijzerplaten, weegschaal, thermometer, vormleer-tekeningen, MAB-blokjes, breukfiguren, munten en biljetten. Eén mechanisme: px-geometrie blijft de `viewBox`, het element is in `em` gemaat in een doos met `font-size: var(--sheet-size-math)` (`PX_PER_EM_AT_DEFAULT = 17.33`, dus 13pt = pixelgelijk aan vroeger, per familie A/B-gemeten; 18pt = ×1,385); tekst ín zo'n SVG in viewBox-eenheden (anders dubbel geschaald); kolomaantallen via `useSheetSizePx('math')`; MAB en breuken plafonneren de figuur op wat de kolom aankan. Op ware schaal (meetlatten, vormleer-tekenraster, breukfiguur in cm) blijft px. Bijvangst: breuken liep op ½ al over (392/338) en weigert die breedte nu eerlijk, hoeveelheid kan wél ½; weegschaal las zijn celbreedte nooit. Bij 16pt kan een standaardblok MAB/breuken/teruggeven groter dan een pagina worden — de banner met "Verklein dit blok" vangt dat. Nieuwe BUGS-regel: vormleer-eigenschappen-mini steekt uit zijn tabelcel. viewer/README regel 2 aangescherpt. Nog te doen voor de eigenaar: breedtematrix (`npm run matrix`) herdraaien, de §1–§4-review, en nakijken of 16pt als schuifmaximum volstaat.

**2026-09-13 (4)** — Reviewronde §0/§5, batch 2 (twee Opus-agents). Slepen: elk kandidaatblok krijgt tijdens de sleep een accent-kader met drie gekleurde banden, het blok onder de aanwijzer licht zijn zone op en dimt de andere twee, en een hintstrip boven het blad zegt wat de drie zones doen; in het Overzicht blijft de domeinlijn van een rij zichtbaar als doel (signaal via outline/box-shadow i.p.v. border) en een sleep selecteert geen tekst meer. Koptekst: een titel "midden" staat altijd op een eigen regel onder de naamvelden (de flankerende variant is weg). Nummering: `skipNumbering` per blok ("Meetellen in de nummering", alleen bij verborgen opdrachttekst), één pure `numberBlocks()` voor blad, Inspector-chip en miniatuur (die nummerde op array-index). De overloopbanner van een te groot blok biedt nu "Verklein dit blok" (zet fitToPage, selecteert, opent Opmaak) en "Splitsen". Plus `useSheetSizePx()` als voorbereiding op batch 3.

**2026-09-13 (3)** — Reviewronde §0/§5 uit `REVIEW.local.md`, batch 1 (twee Sonnet-agents, Fable reviewt). Vergelijken "vul in < > =" op ½ liep over de ¼-buur heen: het kolomaantal volgt nu `useBlockWidth()` (1-up op ½). Knoppenrail heeft een eigen ondergrond (was onleesbaar over een buurblok). De versoepelingsmelding onder Genereer is een callout met icoon i.p.v. een grijze hintregel. Ruitjesraster tekende een vaste px-breedte en pinde zo zijn eigen tier op vol; nu `width:100%` + `maxWidth`, dus ½ en ¼ beschikbaar (schrijflijnen en kader maten al goed). Onthoudkader: lichte opmaak (`**vet**`, `*cursief*`, `__onderstreept__`, `1.`/`-`-lijsten) via de pure `kaderMarkup.tsx` + B/I/U/1./•-knoppen en een hoger tekstvak; oude teksten renderen onveranderd. Batch 2 (overzicht-sleeplijn + dropzones, koptekst midden op de tweede regel, nummering overslaan, "Verklein"-knop in de overloopbanner) en batch 3 (SVG-figuren volgen de lettergrootte-schuiven) volgen.

**2026-09-13 (2)** — Nieuw type **Hoofdrekenen › Gemengd** (natuurlijke + decimale getallen), gebouwd door drie agents tegen één contract. Een blok mengt *varianten* (bewerking + optionele voorinstelling: + / + compenseren / − / − compenseren / × / × tienvoud / : / : tienvoud), `mix` willekeurig of om de beurt; `perVariant[id]` houdt schaarse overrides per tabblad die `effectiveBlockFor` over de gedeelde instellingen legt en dan door de bestaande hr-std-generator (met versoepelingsladder, vereniging van de meldingen) haalt. Inspector: Algemeen + tabbladen per gekozen variant die de bestaande Optellen/Aftrekken/…-plugins ongewijzigd monteren via een nieuwe `ConstraintScope`-context (leest samengevoegd, schrijft alleen in het tabblad; "Zoals algemeen" wist). Op het blad is het bewerkingsteken van elke oefening klikbaar: kies een andere variant en die ene oefening wordt opnieuw gegenereerd (`regenerateExercise`, met undo; elke oefening draagt nu `variant`). Breedtematrix: past op vol/½/¼ zoals hoofdrekenen. Bijvangst: hoofdrekenblokken begonnen al maanden als "Natuurlijke getallen:" i.p.v. "Reken uit." (preset-match op `optellen` terwijl de typeId `hr-std-optellen` is) — gefixt. Reviewfixes eerder vandaag: operator en `=` in vaste kolommen met gelijke lucht, kalender-antwoordlijnen op één kolom, compenseren-tussenstap op de operandlijn. 800 tests. Gepusht naar origin/dev als veilig punt.

**2026-09-13** — Deel 7 + afsluiting. (Na review: operator en `=` staan weer in vaste kolommen met gelijke lucht vóór en na het teken — de tweede term links uitgelijnd in een vak zo breed als de breedste term; de rechts uitgelijnde eenheid uit B1 liet de `+` verschuiven bij een 1-cijferige term.) **Breedte per blok gemeten** (7a): `min-content`-probe per cel, de tiertabel is alleen nog first-paint-fallback, veto's (`VETO_MIN`) blijven editoriaal; de breedtekiezer zegt in px waarom iets niet past. **Knoppenrail** buiten de geknipte pagina (portal, compact). **Opdrachttekst tonen** per blok. **Lettergrootte-tokens** `--sheet-size-math` 13pt / `--sheet-size-text` 15pt met schuiven op de Blad-tab; alle viewers op factoren (S1–S4, 250 sites), geverifieerd met een pixel-diff van 496 renders (124 bladen × 2 breedtes × oplossingen aan/uit): 0 kapot, 1 echte reflow (ordenen decimaal op ½, leesbaar), 10 valse meldingen door de geld-RNG (nu seedbaar). **Pointer-sleep** vervangt native HTML5-drag volledig (de "Claude in Chrome"-extensie bevroor de tab), nu met **drie zones**: hierboven invoegen / wisselen / hieronder invoegen, ook in het Overzicht. **Verticale audit** (7f) vond drie echte oorzaken: een gridcel rekt mee met zijn rij (kort blok naast lang blok kreeg de lange hoogte), de lusbreker telde per cel i.p.v. per meetronde (elk blad van 10 blokken viel stil op de eerste paint), en exact-passende pagina's kantelden op float-afronding — plus paginalabel niet meer afgeknipt, koptekst-slack weg en kopmarge 12→8 mm (ROW_BUDGET 37), opdrachttitel wikkelt, tekengrootte-schuiven van koptekst/opdrachten bereiken de tekst nu echt. **Topbalk** (7h) laat labels pas vallen als de balk écht overloopt. **Opruiming**: kapotte Roboto-.ttf's (waren GitHub-HTML) + @font-face + fontsource-devDeps weg, `validators.ts`, `pagemodel-preview.html`, matrix-CSV's, 12 ongebruikte exports, `blockLayout.ts` naar `services/layout/`, `npm run check|matrix|font:baseline|font:compare|height:audit`, viewer-README met de vijf regels, CLAUDE.md herschreven met werkregels voor agents, klascement.md naar `.claude/docs/` (de entry van 12/9 die zei dat het weg was: het is die dag opnieuw geschreven). Bewust gelaten: `RELEASE_SUMMARY` (banner leest alleen RELEASE_VERSION — beslissing eigenaar). Voor Ruben: `REVIEW.local.md` (gitignored) met alle bladen in sidebarvolgorde en rood/geel/groen. Later: demo-video, SEO-pagina's, `/about` als HTML i.p.v. modal.

**2026-09-12 (6)** — Avondsessie, gepauzeerd op sessielimiet. Slepen bevroor de tab in Rubens normale Chrome-profiel maar niet in incognito: oorzaak is de "Claude in Chrome"-extensie die native HTML5-drag-events kaapt. Tussentijds gelandt: kleine sleepghost i.p.v. het hele blok rasteren (2c55714), stroomonderbreker voor een meet→pak-lus (na 12 repacks/s 1,5 s geen metingen, waarschuwing noemt de cel; 86e2d2e), vastblijvende dropzones gewist op elk einde-van-sleep-signaal (04588a4), paginahint uit de gridflow (zorgde voor een valse "loopt 141px over"). Besluit: native DnD verdwijnt, pointer-events-sleep met eigen ghost + randscroll (7e, loopt bij pauze) — kern voor leerkrachten, mag niet breken. Ook lopend: 7c "Opdrachttekst tonen" per blok. Gepland (Part 7 in het planbestand): 7a inhoud-gemeten minimumbreedte (½/¼ niet meer onterecht grijs), 7b knoppenrail buiten de geknipte pagina + compacter, 7d lettergrootte-tokens (13pt cijfers / 15pt opdracht, schuiven op Blad-tab, viewers als factor van de basis). Nog uitstaand voor de eigenaar: echte pdf-print, REVIEW-CHECKLIST.

**2026-09-12 (5)** — Bugronde (deel 6): alle 21 punten uit BUGS.md in vijf agent-batches, 28 code-commits, 742 tests. **Blad**: kopmarge 16→12 mm (scherm én print, ROW_BUDGET 35), kolomlijn start bij de titel en de goot wordt 16px breder als hij aanstaat, operator + tweede operand vormen één rechts uitgelijnde eenheid (gelijke lucht vóór en na `+`, geen "+315" meer tegen het getal), ordenen wikkelt alleen tussen getallen en stapelt onder 200px, 688 is de enige breedtewaarheid. **Generatoren**: splitsen geeft nooit 0 of het totaal als deel; hoofdrekenen heeft een versoepelingsladder (voorinstelling → cijfermaskers → brug → aantal termen; strikte instellingen eerst, opgeslagen instellingen blijven onaangeroerd) met een melding onder Genereer ("Instellingen versoepeld …" / "Slechts N oefeningen mogelijk …"); verbanden breidt noemers uit; een crashende generator meldt "Kon geen oefeningen maken" i.p.v. stil een leeg blok; breedte mag onder curriculumslot; lege geld-teruggeven-accordeon weg; één `OpSetting`-type; 26 plugins op `useConstraints`; vitest op `pool: 'forks'`. **¼-breedte**: derde renderer-trap `tight` onder 200px (antwoordlijn flext, intrinsieke operandvakken), patronen stapelen verticaal, plaatswaarde/deelbaarheid compacter → 32 van 59 types passen op ¼ (was 24), veto's per instelling in `minWidthUnits` (decimaal, compenseren-tussenstap, plaatswaardetabel). **Oplossingskleur**: `--ink-solution` + `solutionText` (rood én vet) in 37 viewers. **Blok splitsen** (eigenaarsbeslissing: leerkracht bepaalt de indeling, nooit automatisch): `splitBlock(id, atIndex)`, schaar-knop met popover "Splitsen na oefening N" (N standaard = wat nog op de pagina past, gemeten uit de `.print-row`-hoogtes), hint onderaan een pagina met > 72px witruimte. **Slepen vanaf overal**: `draggable` gaat aan bij mousedown buiten invoervelden, de greep blijft als zichtbare affordance. **Passend maken**: per blok "Verklein om op één pagina te passen" (zoom tot 0.7); Ctrl+P en `beforeprint` lopen nu via het app-printpad. Bevinding: een blok groter dan een pagina liep óók op papier nooit door (`.page-sheet` knipt in print) — de banner zegt dat nu eerlijk en verwijst naar schaar of verkleinen. Nieuw in BUGS.md: geen lettergrootteschaal op het blad (12 px-waarden in gebruik; eigenaar kiest tokens), en de lijst van wat nog niet op ¼ kan.

**2026-09-12 (4)** — Deel 2b: de ~50 familiegebonden Opmaak-controls van Inspector verhuisd naar hun eigen config-plugins achter nieuwe `EXERCISE_UI`-slots `StyleConfig` / `AdvancedConfig` (Inspector 1393 → 724 regels, geen typeId-vertakking meer; 60 voor/na-screenshots byte-identiek op antialiasing na). `scaffolding` en zes andere sleutels per familie getypeerd; `BlockConstraints`-index nu `unknown` i.p.v. `any` — een ongenoemde sleutel lezen is een compileerfout, nul restcasts nodig. Daarnaast: splitsen/ordenen/procenten(/breuken-rangschikken) lazen de celbreedte niet en legden altijd 2–5 naast elkaar, waardoor ¼ alleen met één oefening kon; nu stapelen ze en staat hun tier op ¼ (breedtematrix herdraaid met `--only`). Cijferen ook naar ¼ (zat op de fallback). Eigenaarsnotities in BUGS.md: kopmarge te hoog, kolomlijn slecht getekend, operatorafstand ongelijk, meer types naar ¼ (hr-std compact-modus), splitsing met deel = totaal.

**2026-09-12 (3)** — Supervisor-ronde (Fable stuurt, twee Opus-agents coderen; alles op `dev`, per stap gecommit). **Tests**: vitest erbij (`npm test`, 695 tests in ~6 s): generator-matrix over alle 59 typeIds × elk sidebar-blad × pairwise-combinaties uit het nieuwe `src/config/constraintSpace.ts` (spiegelt de keuzelijsten van de config-plugins — SYNC), antwoordcontroles per familie, packer, persistence (incl. v2→v3-migratie), viewer-smoke op drie breedtes, store (swap/undo/lock). Twee generatorbugs meteen gevonden en gefixt: MAB crashte bij leerjaar 6 (`Array.from({length: 1e10})`), rekenvolgorde gaf `[]` bij alleen +/− met twee bewerkingen. **Constraints getypeerd** (8 commits): `constraintTypes.ts` met 43 familietypes, `MathBlock<C>`, registry-rijen via `row<C>()` (ving meteen drift: `mentalMathDefaults` gaf ×-sleutels aan +-blokken → gesplitst), elke generator/viewer/plugin narrowt één keer aan de ingang, plugins via `useConstraints`; `constraints: any` is nu `BlockConstraints`. Index blijft voorlopig `any` omdat Inspector's Opmaak-tab ~50 familiesleutels inline leest — deel 2b (StyleConfig-slot per familie) loopt. **Meten-dan-pakken** (zie entry 2). **Slepen op het blad**: greep in de blokwerkbalk, twee zones per doelblok — boven "Hier invoegen", onder "Wisselen" (`swapBlocks`) — met undo; Overzicht-drag had een off-by-one bij naar beneden slepen, gefixt. **Breedtematrix**: `scripts/width-matrix.mjs` rendert elk type op 4/2/1 eenheden en meet overloop + hoogte; 14 types mogen nu smaller (o.a. kalender, MAB, geld-wissel, breuken-bewerken, deelbaarheid op ½; klok, geld-herkennen, temperatuur op ¼), 11 krijgen `minWidthSingle`, 6 editoriale veto's staan met reden in de `LAYOUT`-header; `rowUnits` herijkt uit de metingen (nu enkel first-paint-fallback). Steekproef van vier verruimde types op ½ nagekeken: leesbaar. **Bugbacklog**: `.claude/docs/BUGS.md` — alles wat gevonden maar niet gefixt is (over-restrictieve hr-std-combinaties, ordenen-wrapping op ½, ondeelbare blokken laten witruimte, `ScaledBlock` krimpt niet onder zoom 1, vitest faalt vanuit Git Bash), regel in CLAUDE.md: daar loggen, niet stil fixen. Nog te doen door de eigenaar: REVIEW-CHECKLIST (96 bladen) + echte pdf-print van een blad met ½/¼-blokken.

**2026-09-12 (2)** — Paginaraster van 6 naar **4 eenheden** (vol / ½ / ¼; het derde is weg — een derde A4 is te smal om te lezen en 31 van de 59 types stonden toch vastgezet op volle breedte). Tiers in `blockLayout.ts` in kaart gebracht als 6→4, 3→2, 2→2: nooit smaller, want overlopen is de gevaarlijke richting. Hetzelfde GETAL betekent op elk raster iets anders, dus het bestandsformaat is versiegestuurd: `WORKSHEET_FORMAT_VERSION` 3 + een zuivere, idempotente `migrateWorksheetFile` die onder v3 de `widthUnits` omzet — aangeroepen bij bestand, deellink, autosave en elke bewaarde preset; `loadWorksheet` houdt een defensieve normalisatie voor bibliotheekkaarten die nooit door een geversioneerde parse gaan. **Schermgeometrie gelijkgetrokken met de print**: de schermpagina had eigen paddings (34/8/26px) terwijl de print 16mm/4mm/8mm gebruikt, dus de schermbody was ~38px hoger en een laatste rij die op het scherm paste kon op papier onder de voettekst lopen; `.page-sheet` heeft nu een vaste `height` i.p.v. `min-height` (een pagina die kan groeien heeft geen budget om tegen te meten). `blockContainer` telde `marginBottom: blockSpacing` bovenop de eigen `gap` van het raster — elke goot werd dus dubbel getekend en enkel begroot. Cellen worden nu **expliciet** geplaatst op de rij en kolom die de packer koos; met auto-plaatsing vult de browser een gat in een eerdere rij op en verschuift een blok stilletjes weg van de rij waartegen de paginering gekost was. **Meten-dan-pakken**: nieuwe `useMeasuredHeights` (React-state in App, bewust niet in de store — het beschrijft hóé het blad rendert, niet wát het blad is) levert de echte celhoogtes en het echte bodybudget terug aan de packer, die zuiver blijft doordat App ze als callbacks injecteert. Dat lost de lege staarten én de rode overloopbalk in één keer op: de schatting blijft alleen nog het eerste-verfbudget. Convergentie is aantoonbaar (celhoogte hangt af van blok/breedte/witruimte/docSettings en nooit van de plaatsing; breedte volgt instellingen en nooit een meting), met 2px-drempel en een dev-only waarschuwing boven 5 herpakkingen per seconde. Afdrukken wacht nu twee animatieframes zodat het dialoog dezelfde paginering ziet als het scherm. De Inspector zegt onder de breedtekiezer wanneer een blok verbreed moest worden (`PackedBlock.promoted` werd berekend en nooit gebruikt). Tests: 689 groen, incl. v2→v3-migratie en gemeten-hoogte-overrides in de packer.

**2026-09-12** — Docs consolidated and put under version control. The `*.md` / `!README.md` lines are gone from `.gitignore`, so every doc is tracked again; ARCHITECTURE, UI-GUIDE, UpdateState and REVIEW-CHECKLIST moved to `.claude/docs/` (all `src/` links rewritten to `../../src/`). Deleted the executed or empty scratch docs — AUDIT-FINDINGS / AUDIT-FIXLOG / AUDIT-VISUAL-FINDINGS (folded into one dated 2026-07-07 entry above), PAGEMODEL-PLAN (shipped 2026-09-09), PLANNED-FEATURES (its one item was dropped), Bugs (empty) and klascement. ARCHITECTURE: §11 file map rewritten from the real tree (PanelShell + useMediaQuery out, pagePacker / blockLayout / PageSheet / regionStyle + the config and ui files that had drifted in), §15 folded into §9 so there is one print section (real pages, packer decides breaks, `break-after: page`), §14 marked whiteboard-branch-only. CLAUDE.md cut 653 → 139 lines: the directory tree, exercise-type table, state table and type definitions are now one-line pointers into ARCHITECTURE, with a docs link table at the top. README's three stale claims fixed (one theme, real-pages print instead of the `<table>`, 51 registry types as a domain-level summary instead of a partial table). The doc-sync hook is purely git-diff based again — the mtime fallback existed only because the docs were untracked — and now matches `.claude/docs/ARCHITECTURE.md`.

**2026-09-10 (4)** — Selectiekader zonder omlijning (alleen de zachte tint + halo; de ring tekende een harde blauwe doos rond het werk, precies het tegendeel van "rustiger voor de ogen"). **Doc-sync-hook gerepareerd**: hij bepaalde via `git diff` of ARCHITECTURE.md/CLAUDE.md waren bijgewerkt, maar deze repo negeert *.md (README uitgezonderd), dus die twee zijn per ontwerp untracked en de diff is blind voor ze — de check kón dus nooit slagen en vuurde bij élke structurele wijziging. Nu een mtime-fallback: docs die minstens zo nieuw zijn als het nieuwste gewijzigde structurele bestand tellen als bij. Beide richtingen getest (stil bij bijgewerkte docs, exit 2 na `touch` op de store). **Release**: `main` én `dev` fast-forward d4f1088 → 0a3dfac, dus de hele helderheidsronde staat live (operatorglyphs, Ubuntu voor woorden, standaardopdrachten, antwoordlijnen op blokbreedste antwoord, stepped-groepering, vette oplossingen, verplaatspijl-fix, undo/redo-sneltoetsen + scroll-naar-blok, eigen kleur, kolomscheidingslijn). **Voor vanavond genoteerd**: in de 2-op-1 opstelling plakt de operator tegen een driecijferige tweede operand ("+315", "+676") terwijl er bij één/twee cijfers wél lucht staat ("+ 51", "+ 9") — de compacte operandcel is rechts uitgelijnd en de operatorgap is daar te krap (zie COMPACT_OP_GAP in MathBlockRenderer, waar de comment dit probleem al beschrijft voor de brede variant).

**2026-09-10 (3)** — Selectiekader en kolomlijn op `ui-change`. De blauwe selectie plakte tegen het opdracht-kader sinds de horizontale inspringing eraf ging (nodig om het kader op de 53px-inhoudsrand te krijgen). Eerste poging was véél te dik: `--accent-soft` is maar 12% alpha en stond vóór de massieve ring in de box-shadow-lijst, dus die ring scheen dwars door de hele band — het las als één vette blauwe rand. Ring staat nu eerst en is 1px, met 6px bleke tint erbuiten; de tint doet het werk, de lijn markeert alleen waar het blok eindigt. Bewust een outset-shadow en géén padding: padding zou de inhoud verplaatsen en de kader-uitlijning weer breken. (Eerdere comment beweerde dat een ring buiten het blok zou worden afgeknipt door `overflow:hidden` — dat klopte niet: overflow knipt op de padding-box, en de eigen 53px zijmarge van het blad laat ruim genoeg over.) Blok-werkbalk schoof mee naar 12px zodat de knoppen de halo niet raken. **Nieuw**: `docSettings.showColumnDividers` — verticale scheidingslijn in de goot tussen blokken die een rij delen, de tegenhanger van de bestaande horizontale "Scheidingslijn tussen oefeningen". Getekend als pseudo-element ín de goot (een border op het blok zou layoutbreedte innemen en het kader opnieuw van de inhoudsrand duwen), gecentreerd op `blockSpacing` dus volgt die schuif, en tekent alleen waar een blok écht een buur links heeft — op een eenkolomsblad dus een no-op i.p.v. een zwevende streep. Doc-sync-hook sloeg aan op de store-wijziging: vals alarm, want alle *.md staan in .gitignore en de hook kijkt naar `git diff`, dus hij ziet een docupdate per definitie nooit. Docs zijn wél bij: `showColumnDividers` in beide tabellen, plus alsnog het neveneffect van `setActiveSelection` (een echte blok-id zet ook `inspectorTab: 'oefening'`) en de returnwaarde van undo/redo verplaatst van de slice-tabel naar de historieregel, want het is geen state.

**2026-09-10 (2)** — Helderheidsronde op `ui-change` (níet naar main; `main`/`dev` staan op d4f1088 met de topbalk-fix). **Operatoren**: vier viewers drukten al een echt minteken en een maalkruis af, de hoofdrenderer en het cijferraster drukten de opgeslagen ASCII rechtstreeks af — hetzelfde blad kon dus een koppelteken én een minteken tonen. `formatters.ts` bezit nu de tabel (`OP_GLYPH`/`opGlyph`), de vier lokale kopieën importeren ze. Uitlijning is veilig: geverifieerd ín de woff2 dat U+2212 in de gebundelde latin-subset zit en dezelfde 650-eenheden breedte heeft als het koppelteken (fontTools). `x` blijft een kruis en `:` blijft een dubbelpunt (lagereschoolconventie); de opgeslagen operator blijft ASCII. **Opdrachttekst**: een nieuw blok begon met zijn eigen typenaam + dubbele punt ("Aftrekken:"), een categorie i.p.v. een opdracht. `TYPE_SUGGESTIONS` dekt nu de reken-, getalbegrip-, meet- en meetkundefamilies en `defaultInstructionFor()` neemt de eerste regel; families zonder match houden de oude label-fallback. Volgorde is nu betekenisvol (eerste match wint), dus specifieke prefixen staan boven bredere. **Twee lettertypes**: Ubuntu erbij via @fontsource (lokaal gebundeld, offline-safe) met tokens `--font-sheet-text` (woorden: instructie, koptekstvelden, bladtitel, voettekst) en `--font-sheet-math` (alles met cijfers blijft monospace, want daar hangt de kolomuitlijning van af). De print-voettekstregel had een eigen `!important`-font — die leest nu hetzelfde token, anders zou het voorbeeld liegen tegenover het papier. De titelbreedte-schatting die inline-flank vs gestapeld beslist ging van 15px/teken (mono) naar 13 (Ubuntu bold 24px ≈ 12). **Stepped**: de stapregels van één oefening staan 32px uit elkaar en de volgende oefening begon precies even ver — drie stappen lazen als drie sommen; er staat nu ruimte onder een stepped-oefening. **Oplossingen** zijn nu ook vet: op een zwart-witprinter is het rood gewoon nóg een grijs. Dekt de vier bewerkingen; 37 viewers zetten het rood nog inline en willen een gedeeld token (aparte klus). **Schrijfruimte** standaard 14→18px (alleen nieuwe blokken; de schuif 8–40 onder Opmaak verandert niet). **Antwoordlijnen** volgen niet langer de layout-preset maar het breedste antwoord ván het blok — bewust per blok en niet per oefening, want een lijn op maat van zijn eigen antwoord verklapt hoeveel cijfers je moet schrijven; de rijbreedte-schatting voor 1-op-2 leest hetzelfde getal i.p.v. zijn eigen 94px. **Verplaatspijlen**: de guards vergeleken `blockOrder` (het gedrukte opdrachtnummer, begint op 1, bladonderdelen delen het vorige nummer) met `blocks.length` — het eerste blok bood dus altijd een dode "omhoog". Nu een aparte id→positie-map. **Overzicht** toont de instructie onder de typenaam (rij ↔ blad matchbaar op wat erboven staat) + aria-labels. **Undo/redo** geven nu de id terug van het blok dat veranderde; de balk scrolt ernaartoe en laat het oplichten (een undo buiten beeld leek "er gebeurt niets"), en Ctrl+Z/Ctrl+Y/Ctrl+Shift+Z zijn eindelijk écht gebonden — de knoppen beloofden die sneltoetsen al vanaf dag één zonder handler. **Kleuren**: SwatchRow kreeg een eigen-kleur-chip (school/clubkleuren stonden niet in de vaste lijst). **Hulp**: rondleiding is nu de primaire actie bovenaan i.p.v. een omlijnd knopje in de hoek, en de hulptekst staat in de woordenletter. Bewust níét gedaan: tekstlabels op de vijf blok-icoontjes (die strip staat op `left:100%` binnen `overflow:hidden`, labels worden afgeknipt — vraagt om de werkbalk ín het blok te zetten, een layoutbeslissing) en een aparte voorbeeldmodus (het blad ís het voorbeeld; de chrome verschijnt alleen bij hover). Twee meldingen uit de Chrome-run waren onjuist: de omlaag-pijl bestond wel (alleen verborgen op het laatste blok) en er staan nergens letterlijke "..." in UI-teksten.

**2026-09-10** — Topbalk liep over zichzelf op laptopbreedte. De balk is niet volle breedte: hij zit in de middenkolom tussen twee vaste panelen (286 + 366px), dus hij krijgt altijd `100vw - 652px`. Met alle labels wil hij ~1490px, en op 1295px drukten de drie zones dus letterlijk over elkaar. Twee oorzaken: het middenspoor was `auto` (weigert te krimpen onder zijn inhoud, dus een lange bladnaam duwde de knopgroepen in elkaar in plaats van zichzelf af te kappen) → nu `minmax(0, auto)`; en er was geen inklapvolgorde → nu vier tiers, goedkoopste eerst: beta-chip + bewaartekst (de bol blijft) → labels op secundaire knoppen → bladnaam → undo/redo (mag als laatste weg, Ctrl+Z werkt nog). Afdrukken houdt zijn woord op élke breedte via `.keep-label` (IconButton kreeg daarvoor een `className`-prop). Géén `overflow:hidden` op de groepen: dat zou de dropdownmenu's afknippen. Verder: Blad-subtabs zijn nu underline-tabs i.p.v. een tweede pillenrij (twee identieke segmented controls boven elkaar zeiden niets over welk niveau je bediende) · een blok selecteren springt naar Oefeningen (vanaf Blad kostte elke selectie een extra klik) · een geselecteerde Overzicht-rij houdt zijn domeinrail (`rowActive` zette `borderColor`, wat alle vier de zijden overschilderde en de rail opslokte — nu een inset-ring: ring = toestand, rail = identiteit). **Release**: `main` én `dev` fast-forward van 864f90c naar d4f1088 (36 commits: het paginamodel, de UI-overhaul van gisteren en deze fix). Bewust gepusht zonder afdrukcontrole vooraf, op vraag van de eigenaar; alles daarná blijft op `ui-change` tot hij het getest heeft. Volgende ronde (getypte plan): echte minusteken (alleen als Azeret Mono U+2212 bevat — `x` en `:` blijven, dat is de lagereschoolconventie), proportioneel lettertype voor instructies/kop/voet maar nooit voor wiskunde, instructionPresets als standaardopdracht, meer schrijfruimte, oplossingssleutel die grijstinten overleeft, verticale scheiding bij stepped, antwoordlijnen op cijferaantal i.p.v. layout-preset, blokwerkbalk-labels + de op/neer-bug (de guards gebruiken het opdrachtnummer als array-index), en polish.

**2026-09-09 (4)** — UI-overhaul op `ui-change`, met een nieuwe leidraad vóór de code: [UI-GUIDE.md](UI-GUIDE.md) is volledig herschreven rond acht toetsbare regels voor de leerkracht die RekenRaak een paar keer per trimester opent (woorden van een leerkracht als labels · toon het resultaat i.p.v. de term · alles zichtbaar maar gegroepeerd · één primaire actie · geen icoon zonder woord · domeinkleur = identiteit, `--accent` = toestand · kleur nooit alleen · schermchroom drukt nooit af). **Eén thema**: donker én "Contrast" zijn weg, met het hele `data-theme`-mechanisme; de tokens staan op kale `:root`. Contrast diende slechtzienden, niet kleurenblinden — dat is nu opgevangen ín het ene thema (separator .12→.20, `--text-muted` #6C6C70→#5A5A5F) en kleurenblindheid door regel 7. **Palet**: warm off-white canvas met wit blad, `--accent` weg van systemBlue (het botste met het domein Bewerkingen en haalde maar ~3,6:1), `--danger` niet langer identiek aan Getallenkennis, en `--domain-<naam>-line/-soft` afgeleid met `color-mix`. **Domeinkleuren leven eindelijk**: rails in de sidebar, in de Overzicht-rijen en op de Inspector-chip — telkens mét het domein in woorden, nooit een losse bol. **TopBar**: één rij op `--bar-h` zodat de drie kolomkoppen op één lijn staan (`--control-h` voor elke knop en elke tab), ≡ en ⚙ samengevoegd tot één **Meer**-menu, destructieve acties dáárheen verhuisd (weg bij Afdrukken), labels op alle iconen, en het wordmerk naar de voet van de sidebar zodat het midden vrijkomt voor de **bladnaam** (beta links, bewaarstatus rechts). **Inspector**: chip die zegt wát je bewerkt (opdrachtnummer + label + domein), tabs hernoemd en herordend naar Oefeningen/Opmaak/Blad met `oefening` als default, en Blad opgesplitst in drie subtabs (Koptekst/Opdrachten/Voettekst) in paginavolgorde. De **Stijl-aanpassen-modal is opgeheven**: zijn drie regio's zijn exact die subtabs, dus de velden staan er nu in (`RegionStyleFields.tsx`) en het tweede, minder accurate voorbeeldje is geschrapt. **Blad = interface**: klikken op de koptekst of voettekst op het blad selecteert het document en opent de bijhorende subtab (`bladSection` in de store; geen historie, niet bewaard). **Print**: opdracht-kaders liepen 21px binnen de 53px-inhoudsrand terwijl kop- en voettekst erop stonden — de horizontale inspringing van de blokcel is nul, wat meteen `cellWidthPx()` eerlijk maakt (die negeerde die 21px al altijd). **Voettekst**: drie instelbare slots + `brandSlot`, zodat de leerkracht kiest wáár "Gemaakt met RekenRaak.be" staat (hij blijft altijd staan). Verder: dubbele Documenttitel weg (staat in de balk), "Ruimte tussen oefenreeksen" van Koptekst naar Opdrachten, uitgeschakelde tabs zien er nu uitgeschakeld uit, koffie-link geschrapt, en rondleiding + Help bijgewerkt (die wezen nog naar de ⚙ en het themamenu). Viewer- en afdrukhelderheid zijn bewust buiten scope gehouden voor een aparte ronde.

**2026-09-09 (3)** — Paginamodel gebouwd op de nieuwe branch `ui-change` (16 commits; `dev` en `main` onaangeroerd op 864f90c, zodat de beta gewoon blijft printen). **Fase A**: één paneel links met tabs — later teruggedraaid, zie shell. **Fase B**: negen viewers lazen een hardgecodeerde `A4_CONTENT_PX = 625`; die wordt nu een doorgegeven celbreedte (`BlockWidthContext`). Bewezen no-op door alle 119 leaves op twee aantallen te meten voor én na (110/119 identiek; de 9 andere wiebelen even hard bij twee runs van dezelfde code). **Fase C**: `blockLayout.ts` (6-koloms raster, GEMETEN rowUnits per type, `minWidthUnits` als functie van de instellingen) + `pagePacker.ts` (zuiver, 17 unit-checks) + `PageSheet.tsx`. De één-tabel-met-herhaalde-thead is weg: elke pagina rendert haar eigen kop en voet en eindigt met `break-after: page`. Geverifieerd: 6 blokken → 2 pagina's op het scherm én 2 pagina's in Chrome's eigen PDF-export. **Fase D**: breedtekiezer (vol/½/⅓) met de te smalle opties uitgeschakeld, en een overflow-badge omdat geschatte hoogtes fout kúnnen zijn. **Bladonderdelen**: vijf niet-oefening-blokken (sectie, schrijflijnen, ruitjesraster, onthoudkader, lege pagina). **Voettekst**: drie slots, links vast op 'Gemaakt met RekenRaak.be', en echte paginanummers — pas mogelijk omdat de packer index en totaal kent. **Shell**: PanelShell + de hover-flyout zijn geschrapt (collega's op 14" laptops liepen erin vast, pin en al); beide panelen staan nu altijd open en het blad zoomt mee (55% ondergrens). Inspector kreeg tabs Blad/Weergave/Oefening; de TopBar is drie zones die de kolommen eronder volgen, met de tabstrips erin en het wordmerk gecentreerd bóven het blad. **Gevonden bugs**: fase B was dood bedraad (de prop werd nooit doorgegeven) — zichtbaar geworden doordat twee types met dezelfde viewer verschillende tiers maten; een pagina kon overlopen op haar eigen laatste rij; een paginavullend blok kon een rij delen; de kostenfunctie zei 1 oefening per rij waar MathBlockRenderer er 2 tekent. ARCHITECTURE §15 + CLAUDE.md bijgewerkt.

**2026-09-09 (2)** — Nakijkronde-fixes. **Stappen 2-koloms bijgesteld**: de regel was een pure breedtetest, waardoor ook 4-cijferige operanden 2-up gingen; nu gecapt op 3 mono-tekens (= onder 1 000) via `STEPPED_2UP_MAX_CHARS`. In én alleen in dat 2-up-raster krimpen de operandkolommen (geen 85px-uitlijnvloer, 16px i.p.v. 26px operatorgat) zodat de gewonnen breedte naar de schrijflijn gaat — gemeten 144px → 226px per tussenstaplijn. `effCellPx`/`effOpGap` sturen celbreedte, operatorspan én de edit-input (die had nog een 70px vloer die uit zijn cel liep). 3-termketens blijven toegelaten en vallen op breedte weg. **Rekenvolgorde uitgelijnd**: expressie + `=` zaten in één auto-breed span, dus het isgelijkteken volgde de lengte van elke som; nu één vaste, rechts uitgelijnde expressiekolom per blok zodat `=` en schrijflijn per kolom op één x staan, plus de ontbrekende breedtecheck (4 bewerkingen tot 1 000 valt terug naar 1 kolom i.p.v. over te lopen). **Vormleer**: een rechte krijgt geen pijlpunten meer (Vlaamse notatie), idem voor de evenwijdige/snijdende/loodrechte paren — dat zijn ook rechten; de halfrechte houdt zijn ene pijl en de loodrecht-vierkantjes blijven. **Proefbanner**: nieuwe `TRYOUT_TYPE_IDS` (16 juli-types) + `TRYOUT_SEEN_KEY`; de banner verschijnt enkel zólang zo'n blok op het blad staat, is wegklikbaar, blijft weg na herladen en is `no-print`. Verder `REVIEW-CHECKLIST.md` aangelegd (95 nog niet door de eigenaar nagekeken leaves, gegroepeerd per sidebar-domein, plus wat al goedgekeurd is en wat bewust niet gefixt is) en twee verouderde regels in CLAUDE.md geschrapt (`services/handig/` en `HandigViewer` bestaan sinds juli niet meer). Playwright-geverifieerd: 0px overflow, Kort/Lang/puntoefening ongewijzigd, geen console-fouten.

**2026-09-09** — Hoofdrekenen "Stappen" mag nu 2 oefeningen per rij. De 2-koloms test in [MathBlockRenderer](../../src/components/viewer/MathBlockRenderer.tsx) hing aan `isInlineShort`, waardoor stepped altijd 1-up bleef en bij kleine getallen de halve bladbreedte verspilde. Er is nu een aparte `steppedRowMin`: operandblok + `=` + een `worklineMinPx` die meeschaalt met het breedste getal van het blok (`max(80, maxChars*CHAR_PX+30)`), want een stappenlijn moet schrijfruimte voor een tussenstap bieden — niet het vaste 94px-antwoordveld dat `rowEstimate` aanneemt. Gemeten breekpunt: 2-up t/m tot 10 000, 1-up vanaf tot 100 000, en 3-/4-term-ketens en de compenseren-tussenstap vallen vanzelf terug naar 1-up; met-rest-rijen negeren layout sowieso. `justifyItems` is voor stepped `stretch` i.p.v. `center` (centreren zou de flex:1-schrijflijn dichtklappen) en `colGap` rekent met de schatting die de 2-up-beslissing nam. Geen nieuwe constraint-key, store-slice of persistence-wijziging; FragmentableGrid regelt het pagineren al. Playwright-verificatie: 0px overflow t.o.v. de print-body-cell op alle varianten, Kort/Lang/puntoefening ongewijzigd. Ook de labeltypo "Antal stappenlijnen" → "Aantal" in de Inspector gefixt.

**2026-07-07 (4)** — Bordmodus iteratie 2 na owner-testronde (7 commits op `whiteboard`). Bugs: inkt-release gehard + BoardErrorBoundary per widget/inktlaag (crash niet reproduceerbaar — HMR-verdacht), frame-rand fix (content bedekte de rechterrand), "Optellen met breuken"-labels. Widget-chroom herbouwd als window-card (typbare titelbalk = drag handle, 🔄/👁/⚙/⧉/🗑, resize-grip; ⚙ opent de inspector i.p.v. selectie). Upgrades: weer met plaats-zoeken (open-meteo geocoding), geluidsmeter → niveau-poster 0-4 + nieuwe werksymbolen-tegelkiezer, datum met live tijd/kleur. Zeven nieuwe widgets: timer, stopwatch, dobbelstenen, ademhaling, groepjesmaker (moet-samen/mag-niet-samen), checklist, stappenplan. Toevoegen-menu = categorieën met tegel-zijpanelen + ★-favorieten (max 6) op de balk + ⚙-bordinstellingen; tekst werd de T-tool. Wiskunde-gereedschap: lege getallenlijn, lege positietabel, honderdveld (tap-kleuren), breukenvisualisatie (cirkel/pizza/lijn), MAB-materiaal (Dienes per plaats) en het geld-sleeppalet (drag-to-create munten/biljetten, tekening/echt). ARCHITECTURE.md §14b + CLAUDE.md board-tree bijgewerkt.

**2026-07-07 (3)** — Whiteboard app "Bordmodus" P1+ built on the new `whiteboard` branch (10 commits; dev untouched — fold-in only when both tracks are done). All code isolated under `src/board/` (own Zustand store, own persistence v1 with autosave/mijn-borden/file-export; only 3 touchpoints outside: view union, App mount, TopBar button). Shipped: full-screen board with bottom toolbar; movable/zoom-scalable widgets (CSS-zoom sizing, `w/NATURAL_W`); **exercise widgets** — any Rekenraak type as a live widget with the real Config plugin (draftBlocks mirror), quick 🔄 genereer + 👁 toon-oplossing, single-add Wiskunde side panel; ink engine (pen/marker/gom, kleuren incl. bewaard, diktes, undo/redo); hand tool (drag zonder selectie); backgrounds (raster/lijnen/schrijflijnen 2+4 met blauwe band/cornell × wit/zwart × grootte); pagination; widgets: tekst, datum, klok (sleepbare wijzers, analoog/digitaal/geschreven tijd), afbeelding, namenkiezer (localStorage klaslijst), weer (open-meteo), geluidsmeter (5 niveaus). ARCHITECTURE.md §14 + CLAUDE.md board section added. P3 (lijn/vormen) en P4 (snappende meetinstrumenten — moet exact) gepland.

**2026-07-07 (2)** — Visual-audit fix batch (6 commits on dev, Playwright-verified per fix, no subagents). Fixed the V-series findings from the print-width visual audit: hoofdrekenen operand columns + edit inputs now scale to the block's biggest number and the 2-up grid drops to 1-up when two rows can't fit print width (V1); breuken kleuren/herkennen render composite denominators as 2D grids (14→2×7, 15→3×5, 16→4×4) and cap prime strips at the column width (V2); getallenas shrinks tick gap + label font at big maxGetal/many ticks, getallenrijen scales its pill font (V3); romeinse prompt/answer columns size to content so niveau 4 fits 2-up (V4); afronden rooster is 2-up again at the default 2 targets (V6); titel-links header fields form a clean right-hugging block (V7). V5 (deelbaarheid 10-divisor tabel) turned out to be a phantom of a miscalibrated test harness — true print content is ~672px (628px inside a block), not 625px; all fixes re-verified at the corrected width. Details + repro per fix in AUDIT-FIXLOG.md (V-series). tsc + eslint + vite build clean.

**2026-07-07** — Audit backlog cleared (23 fix commits on dev, no subagents). Ran the two background audit workflows to completion, merged 82 findings into AUDIT-FINDINGS.md (15 high / 50 medium / 17 low, dedup-collapsed), then fixed each one, one commit per fix, with a how-it-was-fixed + how-to-test entry per fix in AUDIT-FIXLOG.md. Highlights: MAB value 10 renders a tens rod; breukbewerk gemengd answer key matches; breuken-rangschikken/lijnstuk/veelhoek honor their ranges; cijferen omgekeerde-controle correct for 3–4 terms; kettingsommen stale-closure op-repeat fixed; masked number builds never exceed maxGetal (plaatswaarde/vergelijken/afronden, +decimal no-op targets dropped); getallenas/getallenrij no negative ticks; manual-edit recompute matches generators (cijferen decimal division, splitsboom decimals, fixedTotal snap); klok 24h "half 24" + digitaal-tekenen fills an empty clock; deelbaarheid/afronden print-width overflow fixed (rooster 1-up, tabel fits 10 divisors); six viewers widened to fill the page; splitsen/mab/ordenen degenerate masks handled; dead constraints removed. Inherent-settings cases (impossible masks, prime targetDen, whole-euro pools, tiny time pools) documented rather than force-fixed. tsc + eslint + vite build all clean. All audit docs stay gitignored (local review only). Getallenkennis + meten families still not owner-reviewed.

**2026-07-07 (audit)** — Fix-by-fix index of the audit backlog above, folded in from the (now deleted) AUDIT-FINDINGS / AUDIT-FIXLOG / AUDIT-VISUAL-FINDINGS scratch docs so the history survives in one place. **High:** H1 MAB "Tot 10" rendered value 10 as zero blocks → tens rod (`b4b2336`); H2 breuken-bewerken gemengd answer key mismatched the exercise (`9d04814`); H3 breuken-rangschikken ignored the denominator range (`b8eb50b`); H4 breuken lijnstuk ignored minLineLength and overshot maxLineLength (`86e3967`); H5 breuken veelhoek rectangle exceeded maxWidth/maxHeight (`e66ce05`); H6 cijferen omgekeerde-controle line was wrong for 3–4-term sums (`f0ba006`); H7 kettingsommen produced equal adjacent operators via a stale closure (`fd0522c`); H8 afronden rooster 2-up overflowed/clipped in print (`e504d82`); H9 deelbaarheid tabel overflowed at 7–10 divisors (`f3a4ab9`). **Medium:** masked builds exceeded maxGetal + no-op round targets (`67aaece`); breuken shape/amount out of range + hoeveelheid default (`ad0f712`, `7c7f9d5`); getallenas/getallenrij drew negative ticks on natural lines (`ab48634`); manual-edit recompute mismatched the generators (`15edab1`); klok 24h "half 0" + digitaal-tekenen wrong exercise (`dcd2d31`); deelbaarheid givenCount/rasterCount clamps (`abdfa3f`); mab impossible/tiny masks yielded short or empty blocks (`53fff61`); splitsen degenerate totals/masks (`d9ace32`); ordenen stale mask + breuken-bewerken teller cap (`0487786`); getalfunctie/maateenheid pool shortfalls (`c4a1983`); page-width dead space in six viewers (`deb2d32`). **Low:** print-layout polish (`b272c86`); klok/ordenen correctness (`54126a6`); dead constraints + stale comment (`a479d7c`). **Visual (V-series, print-width audit):** V1 hoofdrekenen big-number clipping (`f0d3c67`); V2 breuken kleuren/herkennen strips (`1e82af7`); V3 getallenas/getallenrijen overflow (`04434ed`); V4 romeinse niveau 4 write-lines (`692c1ba`); V5 deelbaarheid 10-divisor tabel — no fix, phantom of a miscalibrated harness; V6 afronden rooster dead space (`71116a5`); V7 titel-links veldenblok (`ad7b49f`). Inherent-settings cases (impossible masks, prime targetDen, whole-euro pools, tiny time pools) were documented rather than force-fixed. **Also dropped:** the one item on the old PLANNED-FEATURES list — an in-app feedback / bug-report button (bug/idee/compliment modal with an auto-attached share link and an html2canvas screenshot) — considered and skipped for want of a delivery channel; the file is gone with it.

**2026-07-06 (2)** — Feedback batch on the new types (7 commits on dev). **Multi-term mental math**: `Equation` gained `operators[]` + `missingIndex`; MathBlockRenderer iterates operands generically; all four hoofdrekenen generators (natural/decimal/rational) support 2–4 termen/factoren (cumulative carry/borrow bridge checks, answer-first division chains, fraction chains). **Presets**: compenseren (+/−) and ×/: met 10-100-1000 became `constraints.preset` flavours with an Oefenvorm row atop the configs (new `HrPresetRow`); the standalone `tienvoud` + `handig-rekenen` types were deleted. Per-term getalopbouw masks (`operandMasks[]`) + per-term max (`operandMax[]`, geavanceerde opties). **Restructures**: kettingsommen → Patronen; Hoofdrekenen renamed (accordions carry "(standaardprocedure)"); negenproef → Cijferen; rekenvolgorde haakjes = GEEN/MAG/MOET + up-to-4 bewerkingen with a guaranteed handig pair, hulplijn removed; omgekeerde bewerking = exercise + controle-write-line (prefill segment); cijferen +/− kreeg a controlelijn-toggle; driehoeken merged into one leaf with both classify axes (geometric ticks) and marks split into gelijke-lengtes / rechte-hoeken (vierkantje-of-haakje) / evenwijdige-zijden toggles; schattend aligned with (H)/(T) prefix; verliefde harten 5-up over full width; StyleBuilder kreeg "Alle stijlen terugzetten". Getallenkennis + meten families untouched (not yet reviewed).

**2026-07-06** — All sidebar placeholder exercise types implemented (18 new typeIds, three commits on dev): tienvoud (×/: met 10/100/1000, reuses Equation+MathBlockRenderer), schattend rekenen, verbanden breuk·decimaal·procent, procenten, maateenheid kiezen, geld-rekenen (korting/winst/intrest roosters), handig rekenen (compenseren/splitsen), rekenvolgorde en haakjes, kettingsommen (reuses patroonExercises+PatroonViewer), getalfunctie, tijdsduur, kalender (maandrooster/datum-rekenen/notatie), controleren (negenproef-kruis + omgekeerde bewerking), oppervlakte (reuses meetExercises + new `area`), weegschaal (dial SVG à la ClockViewer), and the vormleer suite (punt-lijn / hoeken / figuren sharing `vormleerExercises` + one VormleerViewer). `appstructure.ts` now has no placeholders left except the hidden vraagstukken domain. Verified via tsc/eslint/vite build, a 39-case generator smoke (25 runs each), a 37-case SSR viewer render smoke, and a Playwright sweep (21 leaves, zero console errors).

**2026-07-06** — Restored the local-only docs (CLAUDE, ARCHITECTURE, UI-GUIDE, PLANNED-FEATURES, UpdateState, Bugs) from git history (`489e971^`). They were deleted from disk on June 6 by a dev→main→dev branch sync: main still *tracked* them pre-untrack, so the checkout claimed the gitignored copies and the fast-forward merge deleted them. All refs now agree they're untracked, so this can't repeat. NOTE: entries below predate the June 6–9 commits (library views, rekenmethodes, worksheetTemplates, OverzichtPanel, ModalShell); directory trees refreshed only partially — verify against src/ when in doubt.

**2026-06-05** — Exercise text-size control. New advanced-style setting that scales **all body + generated exercise text** via uniform CSS `zoom` (chosen over a ~28-file per-viewer font refactor: a full viewer audit showed text + SVG/box geometry are coupled almost everywhere — clock/thermometer/coin SVG `<text>`, Cijfer `CELL`, cm→px, char-width column math — so font-only would desync). New [ScaledBlock](../../src/components/viewer/ScaledBlock.tsx) wraps each block's opdracht-titel + viewer in [App.tsx](../../src/App.tsx) and **auto-fits to width** (caps the applied zoom via the `scrollWidth/clientWidth` ratio so an enlarged wide block — Cijfer grid, getallenas number line — can't clip in print; floored at 1 so it never adds shrink); block chrome stays outside. Control is in **px** (nominal base 14, 11–18) stored as a zoom factor: global slider in [StyleBuilderModal](../../src/components/configurator/StyleBuilderModal.tsx) (`docSettings.bodyFontScale`) **plus** a per-block override slider in [Inspector](../../src/components/configurator/Inspector.tsx) (`constraints.bodyFontScale`, hidden when locked, "↺ volg blad" reset) — lets e.g. small mental-math text coexist with big clocks. `BODY_FONT_PX` in [printPalette](../../src/config/printPalette.ts). Optional fields → back-compat, no persistence/version change.

**2026-06-05** — Bugfix + render batch. **OOM root cause:** a leftover base/grade `maxGetal` with no matching dropdown option rendered as "—" and reached generators unclamped → memory overflow (also what made the even/oneven rooster *look* like a print bug). Fix: opt-in `clampToLowest` prop on [PopupSelect](../../src/components/ui/PopupSelect.tsx) snaps an out-of-range value to the lowest option and persists it; added to every numeric max dropdown across ~18 config plugins. **Rooster** ([EvenOnevenViewer](../../src/components/viewer/EvenOnevenViewer.tsx)): switched from width-based flex-wrap to a fixed `perRow`-column grid so cells align and "Getallen per rij" is honoured. **Breuk van een lijnstuk** ([FractionExerciseItem](../../src/components/viewer/FractionExerciseItem.tsx)): berekeningslijnen now read `___ cm : ___ = ___ cm` / `___ × ___ cm = ___ cm`; matching cm hint in the [Inspector](../../src/components/configurator/Inspector.tsx) scaffolding picker (lijnstuk only, not hoeveelheid-abstract).

**2026-06-04** — Curriculum + style batch. **Leerjaar mapping** grounded in the Flemish wiskunde leerplan: [gradePresets.ts](../../src/config/gradePresets.ts) now resolves each leaf's minLeerjaar as max(numberType, typeId, label, explicit) with leerplan-correct grades (gehele→L4, breuken→L2, decimale→L4, cijferen→L3, romeinse→L5, …) and leerplan max ceilings (L4≤10 000, L6≤10 miljard). **Sidebar regroupings** ([appstructure.ts](../../src/config/appstructure.ts)): even/oneven lifted out of Deelbaarheid, Veelvouden+Deelbaarheid merged, time subdomeinen merged into "Tijdstip en tijdsduur", Meetkunde reordered before Meten, and fraction +−×: (hr-std-*-rat) moved into a new Bewerkingen›"Bewerkingen met breuken" subdomein (tagged L4). **Style builder**: new [StyleBuilderModal](../../src/components/configurator/StyleBuilderModal.tsx) (size/weight, colors, lines, fill, padding, alignment per header/opdracht-titel/footer with a curated print-safe palette + live preview), driven by optional `RegionStyle` objects in `docSettings` (persist via existing autosave), applied through [regionStyle.ts](../../src/services/regionStyle.ts)'s `overlayRegionStyle`; dropped the footer `!important` on color/size so custom wins. **Mobile block**: now a teaser with a looping desktop demo ([public/rekenraak-demo.mp4](../../public/rekenraak-demo.mp4)) + warmer why-copy.

**2026-06-04** — Large inspector consistency + UX batch. Fixed the stepped ("Stappen") layout so the operand sits on the first answer line. Added shared primitives — [PopupSelect](../../src/components/ui/PopupSelect.tsx) (dropdown for value pickers), [StylePicker](../../src/components/configurator/StylePicker.tsx) (visual-variant modal gallery, wired to MAB Stijl), [BridgeControl](../../src/components/configurator/BridgeControl.tsx) (carry-arrow 'bruggetje' diagram replacing the per-place GEEN/MAG/MOET grid in addition/cijfer + base modal), [InfoTip](../../src/components/ui/InfoTip.tsx) + [SettingLabel](../../src/components/configurator/plugins/SettingLabel.tsx) (one-line ⓘ help on every setting), plus `divider`/`sectionBox`/`select`/`hint`/`miniLabel`/`numInput` style tiers. Swept ~18 plugins: range pickers → dropdowns (`Tot {n}`), `Maximum bereik`→`Maximum getal`, getalopbouw-before-bruggen order + dividers, uniform label weight. TopBar: new `secondary` button variant (Toevoegen demoted), "Nieuw blad" reset, autosave now **auto-resumes** silently (prompt removed) and persists `selectedGrade`. New soft **leerjaar** selector (1–6) in the sidebar: [gradePresets.ts](../../src/config/gradePresets.ts) seeds base difficulty + filters leaves by grade (rat≥L2, geh≥L3, dec≥L4).

**2026-06-04** — Two opdracht-titel UI fixes: a Kader-styled titel with a MAG/MOET/★ prefix no longer double-frames (prefix now renders as plain bold text + a vertical rule inside the box instead of its own bordered pill); added a "Standaardtekst…" dropdown under the Instructie field (new [instructionPresets.ts](../../src/config/instructionPresets.ts), generic verbs + per-type suggestions) so teachers click a common instruction instead of retyping.

**2026-06-03** — Post-ship polish: getalpatronen rows now use a 1fr grid (columns align across rows) with bottom-aligned connectors so the `–` sits on the number line. Getallenrijen gained a **Kader tonen** toggle (Differentiatie, default on) to drop the rounded frame. Deelbaarheid (kleuren) sidebar/mass-add variants are now the **view modes** (Rooster / Omcirkelen / Kleurraster) instead of divisor groups (divisors stay configurable); config segment relabelled Markeren → Omcirkelen.

**2026-06-03** — Shipped "Getalbegrip uitbreiden" to dev + main: bumped the sidebar "Updated" date to 03/06/2026.

**2026-06-03** — Fixed deelbaarheid-kleuren: the divisor is now **random per row** (was a fixed rotation that never changed on Genereer), and the **kleurraster** holds **non-repeating random numbers** with a configurable Maximum getal instead of a static 1..N (so the coloured answer actually varies). Markeren numbers now align in a grid; raster cells widened for up to 4-digit numbers.

**2026-06-03** — Getalpatronen refinements: the `+/−` getalopbouw now spans the full place range from the block maxGetal (D/H/T/E, plus t/h/d for decimals) instead of just T/E; added a **Max decimalen (1/2/3)** selector for decimal patterns (generator precision follows it); **Operatoren invullen** gained an **Operatorweergave** toggle (Enkel teken `+` vs Volledig `+9`). Also bumped the sidebar wordmark slightly larger (height 38).

**2026-06-03** — Two new families + default tweak. **Getalpatronen** (Patronen subdomain, nat/dec/geheel): a row that follows a 1–4-step operation cycle (`+ − × :`) from the left; teacher selects which operations may occur, each with its own operand max + getalopbouw; differentiation toggles draw arrows + a write-line and can pre-fill N operators (slider). Plain line with `–`/arrow connectors, no frame. New `PatroonExercise`, `patroonGenerator.ts`, `PatroonViewer`/`PatroonConfig`. **Deelbaarheid (kleuren)** (under Deelbaarheid): colour/circle the multiples of a divisor in three view modes — rooster (strip), markeren (omcirkel), kleurraster (honderdveld) — with a `Rest berekenen` toggle (`rest: ___`); leaves door 2/5/10 · 4/8 · 3/6/9 · gemengde reeksen (toggle 2–12). New `DeelbaarheidKleurExercise`, generator, viewer/config. **Defaults**: scores + scheidingslijn off, opdracht-nummering on, voettekst fully off. Docs synced; lint + build clean.

**2026-06-03** — Meten/omtrek layout dynamics: `MetenViewer` now picks **1 or 2 columns** based on whether two items fit the print width (`A4_CONTENT_PX = 625`), so long lines/large shapes no longer overflow the sheet. Fixed the `___ cm bij elke zijde` labels by anchoring each outward (transform follows the side normal) so they never overlap the figure, with wider padding when the scaffold is on. Hid the per-side scaffold toggle for **lengte meten** (pointless for a single line) — omtrek only.

**2026-06-03** — Polished the meten/omtrek exercises. **Lengte meten**: the `gegeven` model became a **juist/fout** exercise (line drawn to scale + a stated length the pupil verifies by measuring, circles juist/fout; `claim`/`claimCorrect` on `MeetExercise`); the meten path dropped its per-item "Lengte =" prefix and right-aligns the second column. **Omtrek**: shape constructors now return exact `sides[]` so *hele cm* never drifts to 0,9/1,1; length control relabelled "Lengte per zijde"; bigger label padding so labels don't clip the figure. **Both**: the `___ cm bij elke zijde` scaffold, the answer-mode (Eén lijn / Som van zijden) and a new **answerUnit** (Met cm / Enkel lijn) moved into the Inspector **Differentiatie** card. Docs synced; lint + build clean.

**2026-06-03** — Two batches. **Breuken getalopbouw consistency**: vergelijken representaties now swaps a breuk side's getalopbouw to the teller/noemer widget (`FractionMaxField`) and stores a real `aFrac`/`bFrac`; getallenrijen rational gets the same widget (noemer=sprong, teller=maxTeller) and the decimal mask now matches the step's decimal precision; gelijknamig gained a **Doelnoemer** (fixed common noemer, else KGV) + max-noemer; `BreukBewerkViewer` re-laid-out as a grid so `=`/`→` and writing lines align. **New geometry** under Meten → Lengte en oppervlakte: **lengte meten** (straight line / 0–4-corner polyline) and **omtrek** (driehoek · vierkant/rechthoek/ruit/parallellogram/trapezium/vierhoek · vijf-/zes-/zeven-/achthoek · cirkel-met-middelpunt). Drawn to real cm scale (`MetenViewer`, 1cm≈37.8px), with a meet-model toggle (op-schaal vs lengtes-gegeven), cm/mm precision, per-side `___cm` scaffold, and single-line vs sum-of-sides answer. New `MeetExercise`/`meetExercises`, `metenGenerator.ts`, `MetenViewer`/`MetenConfig`. Also **hid the Probleemoplossend denken domain** from the sidebar (added `Domain.hidden`). Docs synced; lint + build clean.

**2026-06-03** — Refined the round-1 breuken/komma exercises after teacher testing. **Vergelijken → representaties**: restored per-side *Specifieke getalopbouw* (new `leftMask`/`rightMask`, honored by the generator) and reworked the config into 2 columns (Linkerkant │ Rechterkant, divider) each owning its representation + mask. **Breuken bewerken**: extracted the addition "Breuk 1" teller/noemer control into a shared `FractionMaxField.tsx` and used it for gemengd + vereenvoudigen getalopbouw (`maxNumerator`/`maxDenominator`); answer box → single writing line. **Vereenvoudigen**: 2 presets (binnen de maaltafels `tablesOnly` / grotere breuken) + an `allowIrreducible` toggle that mixes in already-simplest fractions. **Verliefde harten** print bug: routed hearts through `FragmentableGrid` (fixed columns) so screen and print match and hearts fragment across pages. Docs synced; lint + build clean.

**2026-06-03** — Added five exercise types/variants. **Splitsen → Splitsboom**: new `layout` on the splitsen typeId — a single split-tree (top + two legs) where the blank slot is random among the teacher-ticked positions (top/left/right), maxGetal ≤ 1000, getalopbouw masks for top + sides. **Getallenrijen** (new subsection under Getalbegrip, 4 number types): number sequences shown in a pill with dotted blanks — getallenas minus the axis line, with a custom jump for every type, a getalopbouw mask on the anchor, and stijgend/dalend direction; reuses the `getallenasExercises` field + `GetallenasExercise` shape with its own generator/viewer/config. **Vergelijken → Breuken & kommagetallen**: new `representaties` subType comparing two tienden/honderdsten values, each side rendered as breuk/kommagetal/plaatswaarde (5E4t)/woorden (5 tienden) via `RepValue` + `representations.ts`. **Breuken bewerken** (new typeId, 3 leaves): gemengd↔breuk (direction toggle), gelijknamig maken (LCM), vereenvoudigen — reuses exported `gcd`/`simplifyFraction`/`toMixedNumber`. **Breuken rangschikken**: replaced the placeholder — orders 2–5 fractions (stambreuken/gelijknamige/gelijknamig-te-maken/speciale curated pool), reuses `ordenenExercises` + `OrdenenViewer` with a new generator/config. Docs (ARCHITECTURE/CLAUDE) synced; lint + build clean.

**2026-06-02** — Rebranded the app **Enderklas → Rekenraak**: new sidebar header (rekenraak wordmark inline-SVG that adapts per theme — `reken` = `--text-main`, `raak` = `--accent` — with a static "Gemaakt door Ruben V.H." line; the old editable site title/subtitle inputs + their localStorage were dropped). The logo is clickable → opens a new stubbed `AboutModal` ("Over dit project"). Renamed all `enderklas_*` localStorage keys to `rekenraak_*` (one-time local reset of autosave/presets/tour/pins), installed the new "rr" favicon set into `public/` + rewired index.html, retitled the tab/banner to Rekenraak, set `package.json` name to `rekenraak`, and deleted the orphaned `enderklas-logo.png` + old `favicon.png`. Docs updated (README/ARCHITECTURE/CLAUDE). Live URL `app.enderklas.be` left as-is pending a domain. Fleshed out the `AboutModal` ("Over dit project") with the full project manifesto + tech + AGPL-3.0 license (sticky wordmark header, scrolling body); extracted the wordmark into a shared `ui/Wordmark.tsx` used by both the sidebar header and the modal; removed the "Gemaakt door Ruben V.H." line from the sidebar (now lives in the modal) and pointed the footer "AGPL-3.0" link at the modal. Added a real `LICENSE` file (AGPL-3.0) + `package.json` license field, and dropped the unused `@react-pdf/renderer` dependency. Fixed the gear-menu popover clipping (portaled out of the `.mac-vibrant` backdrop-filter), centered the sidebar wordmark, and made "reken" use `--text-main` (white in dark).

**2026-06-02** — Apple-HIG retheme: swapped the dark/light color tokens to Apple's exact system colors (systemBlue primary `#0A84FF`/`#007AFF`, neutral systemGray surface ramp, system-color domain accents; purple kept as the *meetkunde* dot), colorblind theme left untouched. Migrated icons from lucide-react to **Phosphor** (`@phosphor-icons/react`) across 10 files and added SF-Symbols-style weight-on-interaction — IconButton + the sidebar theme button thicken their glyph (`bold`) on hover/focus, and `active`/`primary` icons sit bold at rest — via an app-wide `IconContext` default in main.tsx.

**2026-06-02** — Added a far-right search bar to the MassAdd ("Oefeningen toevoegen") modal (sidebar-style, live-filters cards by label/context/variant, with a "geen oefening gevonden" empty state). Added an icon-only "Alle blokken wissen" button to the TopBar (Trash2, confirm-guarded, spaced left of Delen) backed by a new `clearBlocks` store action that pushes history so Ctrl+Z restores.

**2026-06-02** — Responsive rework for small laptops: decluttered the TopBar (autosave prompt moved to a dedicated second row so it never clips; one **Afdrukken** button with a blad/blad+oplossingen menu instead of two; Delen is now icon-only; Exporteer/Importeer/Presets moved into the sidebar gear). Added `PanelShell` + `useMediaQuery` so below 1800px the Sidebar and Inspector collapse to hover/focus flyouts with a per-side pin (localStorage); A4 stays 1:1 so print is untouched. Theme moved to a cycling icon next to Help (removed the Thema row); sidebar subtitle default → "Ruben V.H." (localStorage key bumped v3→v4).

**2026-06-02** — Added a favicon: the Enderklas logo downscaled to a 64×64 `src/assets/favicon.png` (~2 KB, from the 1024² source), linked via `<link rel="icon">` in index.html (Vite hashes/bundles it).

**2026-06-01** — Fixed gray print background (regression): the shadow-clip `.print-scroll` padding wasn't reset in `@media print`, so the inset sheet exposed the gray `--bg-base` (forced to print by `print-color-adjust:exact`). Added `padding:0` to the print `.print-scroll` rule + `background:white` on `.print-root`. Also fixed oppervlakte Hulptabel header/body right-edge drift (header `colW` now matches the collapsed 2-cell column width).

**2026-06-01** — Oppervlakte herleidingen: (1) fixed a generator bug where compounds emitted a square **and** its are-alias in the same place (`47 m² 0 ca`) — standard formats now dedupe to one unit per factor (`gridUnits`, squares win). (2) New oppervlakte-only "Soorten oefeningen" **Vierkant → are** and **Are → vierkant** (ca=m², a=dam², ha=hm²) with an **Are-omzetting** toggle (Enkelvoudig 1-op-1 / Are-stelsel samengesteld) shown when an are-type is picked; reuses the max-getal sliders. New constraint `areMode` (default `samengesteld`); format union + `herleidingenDefaults` extended. Integer-exact (isExact guard) and recompute unchanged.

**2026-06-01** — Herleidingen **Hulptabel** overhaul in [HerleidingenViewer](../../src/components/viewer/HerleidingenViewer.tsx): (1) prompt column got a fixed shared width (≈8.4px/char) so unit cells no longer staircase; (2) `Antwoord = Ingevuld` answer lines shrink when ≥3 parts (volledig) so they stay on one row; (3) **oppervlakte** now renders **2 sub-cells per unit column** (×100 = 2 digits) with columns grouped by factor — the are-units stack on their squares (ha/hm², a/dam², ca/m²) when both enabled. Cellbreedte slider still controls column width. Absolute extreme (all 10 area units + answer) stays user-managed (fewer units / smaller cellbreedte / Antwoord verborgen).

**2026-06-01** — Default theme is now **light** for first-time users (`loadInitialTheme` fallback `dark`→`light`; persisted choices unaffected). First-run banner is now a Dutch **welcome** message (lucide `Hand` icon, not an emoji) instead of the "Nieuw: …" changelog — same once-per-`RELEASE_VERSION` gating + dismiss; App no longer imports `RELEASE_SUMMARY`. Also recorded a decision NOT to horizontally center the MassAdd/curriculum example previews (comment at `ExercisePreview` `scaleWrap` + memory): centering collapses width:100% viewers (number line, thermometer) — left-aligned is intended.

**2026-06-01** — Pre-tester fixes. **A4 sheet**: `height` `max-content`→`auto` so the white card grows past one page (real `<table>` child made `max-content` unreliable); `.print-scroll` got `padding: 8px 48px 48px` so the sheet's 48px-blur drop-shadow isn't clipped (overflowY:auto forces overflow-x to auto). **MassAdd/curriculum previews** ([ExercisePreview](../../src/components/shared/ExercisePreview.tsx)) now scale by **width AND height** (uniform) so tall examples — thermometer, teruggeven diagram — shrink to fully fit the card instead of clipping. **Puntoefeningen** (a + . = c) hide the Kort/Lang/Stappen scaffolding control (Inspector) and always render `inline-short` ([MathBlockRenderer](../../src/components/viewer/MathBlockRenderer.tsx)) across + − × ÷ and natuurlijk/decimaal/rationaal. tsc + lint + build green.

**2026-06-01** — Header/Inspector/Help polish batch. **Autosave prompt** now a centered rounded-rectangle callout in the middle of the top bar (no fill, accent border + `RotateCcw` restore icon; Ja/Nee are outline-only purple buttons) — [TopBar](../../src/components/layout/TopBar.tsx) props `autosaveTitle`/`onAcceptAutosave`/`onDeclineAutosave`. **Koptekst stijl** gained **"Onderstreept"** (order Geen · Onderstreept · Kader) drawing one line under the whole header (`headerStyle: 'geen'|'onderstreept'|'kader'`). **Center title** now flanks a narrow field set (e.g. Naam left / Datum right of the centred title) when it fits one A4 line, else falls back to the stacked layout ([App.tsx](../../src/App.tsx) `renderFields` gained a subset arg). **Footer defaults**: school/klas/leerkracht off by default (only paginanummer on). **Help modal**: teacher "Bugs of suggesties?" now links the contactformulier (forms.gle) instead of X; parents gained a "Steun deze tool" coffee/❤ link to buymeacoffee. **Inspector** right panel top padding dropped to 0 so its first card top-aligns with the TopBar/sidebar tops.

**2026-06-01** — Fixed UI-overhaul regression: all modals were trapped inside the `.mac-vibrant` TopBar/Sidebar because `backdrop-filter` makes an element the containing block for `position:fixed` descendants (only worked in colorblind, where vibrancy is off). New [ModalPortal](../../src/components/ui/ModalPortal.tsx) (`createPortal` → `document.body`) now wraps MassAdd / Preset / Help / Curriculum / Basisinstellingen overlays so they center over the viewport again. Also lifted the "Vorige werkbundel gevonden" autosave banner into the pinned top-bar region (no longer scrolls with the sheet) and removed the "Nieuw: nieuw:" doubling in `RELEASE_SUMMARY`. tsc + lint + build green.

**2026-06-01** — Herleidingen wide-row overflow fix + clearer format labels. The long (compound) conversion side now **wraps** to a second row instead of running off the A4 page. New per-block **Uitlijning** toggle in the *Geavanceerd* accordion (constraint `herleidingLayout`): `uitlijnen` (default — given on the left, shared "=" column) or `compact` (single-part side on the left so the long compound always gets full width; flips `samengesteld→enkel` to `___ mm = 139 km 3 hm …`). "Soorten oefeningen" reverted from illustrative glyph pills to stacked, equal-width **text-label** buttons. tsc + lint + build green.

**2026-06-01** — Herleidingen UX pass + layout fixes. **Layout**: center-column scrollbar now starts at the viewer (TopBar lifted out of the scroll container into a `.print-scroll` wrapper; [App.tsx](../../src/App.tsx)/[appStyles](../../src/styles/appStyles.ts)/print CSS); gear popover no longer clipped (sidebar dropped `overflow:hidden`); Oefeningen (engine) card uncapped (no 520px inner scroll) + inspector/sidebar top padding trimmed to align the column tops. **Herleidingen config**: enkelvoudig max is a smooth linear slider (10–1000); format pills show illustrative blank patterns ("__ cm", "5 __", …). **Viewer**: blocks with wide exercises auto-drop to one full-width column; the Hulptabel is centered with an optional left **prompt** column + a right **= ___ answer** column (blanco/ingevuld/verborgen) + cell width/height sliders (controls in Differentiatie; constraints `tablePrompt`/`tableAnswer`/`tableCellW`/`tableCellH`). **Plaatswaarde** waarde/plaats prompts right-aligned (fixed width) so arrows + answer lines align. Also fixed a **TourOverlay** render loop. tsc + lint + build green.

**2026-06-01** — Herleidingen refinements + **Oppervlakte**. **Two breakpoint max sliders** ([HerleidingenConfig](../../src/components/configurator/plugins/HerleidingenConfig.tsx)): `maxEnkel` (stops 10/100/1000, def 100) for the single formats, `maxSamengesteld` (10…1 000 000, def 1000) for the compound formats. **Compound depth** toggle `compoundMode` 2 eenheden / Volledig (full contiguous decomposition, e.g. `194 mm = __ dm __ cm __ mm`). **Oppervlakte** measure added (squared ladder km²…mm² + are-units ha/a/ca as equal-factor aliases; strict descending `pickPair`; safe-integer guard so huge km² products never float-break — validated 144 000 batches, 0 errors). **Hulptabel** scaffolding control moved from the Oefeningen card to the **Differentiatie** card (Inspector). "Eenheden" → "Maateenheden". **Inline edit on the A4 viewer**: click a given number to edit it, click a unit for a **dropdown** of valid ladder units (no typos); `recomputeHerleiding` restores the conversion and `patchExercise` persists. tsc + lint + build green.

**2026-06-01** — Tree reorg + new **Herleidingen** (metric unit-conversion) family. **Reorg** ([appstructure.ts](../../src/config/appstructure.ts), Getallenkennis): Getalbegrip type order (Plaatswaarde above Splitsen, Vergelijken above Ordenen); Even/oneven moved out of Getalbegrip into **Deelbaarheid**; **Breuken** moved up directly under Getalbegrip; **Romeinse cijfers** moved to the bottom; **Gehele getallen** placeholder removed. **Herleidingen** (lengte/inhoud/massa; oppervlakte stays placeholder) — new [herleidingenGenerator.ts](../../src/services/herleidingen/herleidingenGenerator.ts) (integer-exact metric ladders via `ladderFor`; 4 formats: enkel-getal `1 kg = __ g`, enkel-eenheid `2 l = 200 __`, samengesteld→enkel `3 m 6 cm = __ cm`, enkel→samengesteld `540 cm = __ m __ dm`; dedupe loop; conversions validated over 2000 batches), [HerleidingenViewer.tsx](../../src/components/viewer/HerleidingenViewer.tsx) (aligned `=` column, blank/solution lines, write-units option, optional salmon conversion-table scaffold with/without headers — N rows), [HerleidingenConfig.tsx](../../src/components/configurator/plugins/HerleidingenConfig.tsx) (unit pills, max slider def 9, format pills, write-units toggle, scaffolding segmented). Wired types/registry/exerciseUI; the 3 leaves set measure+units. tsc + lint green.

**2026-06-01** — macOS-faithful sweep + comfort themes + onboarding (branch `ui-overhaul`). **Themes**: Apple-system comfort — dark base lifted `#0d0d10`→`#161619`, off-white text `#ececef`; light base `#efeee9`; colorblind unchanged. **Engine card** unified to the standard `S.card`+`S.cardTitle`; Genereer is now a primary `IconButton` (Sparkles). **M1 segmented**: macOS unified `.seg-group`/`.seg-btn` (one bezel + dividers + tinted selection) for the Inspector segments + theme switch; list-row selectors keep the separated `S.radioBtn`. **M3**: subtle bezel on IconButton. **M4/M4b sidebar**: domains render as source-list section headers — full-width accent-tinted bands (color-mix 12%) with dot + label in the domain accent (replaced the colored left rail). **M5 vibrancy**: `.mac-vibrant` frosted-translucent TopBar + sidebar (backdrop-blur; colorblind stays opaque; Inspector opaque). **P3**: sidebar subtitle now the credit "Gemaakt door Ruben Van Handenhove"; footer dupe removed. **P4**: "Geavanceerd" accordion → a `⚙` gear icon + upward popover in the footer (`BaseSettingsPanel` rewritten). **P5**: new [TourOverlay.tsx](../../src/components/onboarding/TourOverlay.tsx) — interactive first-run spotlight (add→settings→generate→print→WIP/feedback finale), one-time via `enderklas_tour_seen_v1`, replay from Help's "Rondleiding"; replaces AlphaPopup (removed), folding its WIP warning into the last step. Docs synced (ARCHITECTURE/CLAUDE/UI-GUIDE). tsc + lint green.

**2026-06-01** — UI unification + style handbook (branch `ui-overhaul`, follow-up to the Apple-HIG re-skin). **Canon**: `sharedPluginStyles` gained `maskBtn`/`bridgeBtn`/`groupLabel`; Inspector `S.radioBtn` + sidebar `themeBtn` dropped the raised-thumb for the one canonical selected look (accent-soft tint + accent text + 1px accent ring). **Sweep**: every config plugin (incl. `addition/*`, `multiplication/*`, `Cijfer`, all `Geld*`, `Splitsen`, mask-only configs) moved onto the shared helpers — all local `maskBtnStyle`/`radioBtnStyle`/`bridgeBtnStyle`/`pill`/`toggle`/`tableBtn` deleted; hardcoded `#222226`/`#fff`/`white`/`rgba(155,48,255)`/`--accent-bewerkingen` control colors removed; group titles → `groupLabel`; GeldTeruggeven slider → `Switch`. **Hotfixes**: colorblind `--shadow-1: none` (stop the TopBar doubled-edge), MAB default `boxHeight` 60→70 (registry + fallbacks) so thousands don't clip. **Sidebar**: `+` glyph → centered lucide `Plus` (accent on hover); width reclaimed (no domain marginLeft, subdomain font→`--text-sm`, nav labels ellipsis); logo 56→48; footer name on its own line. **Block rail**: delete moved to the bottom behind a divider (anti-misclick). **Features**: click the opdracht title on the A4 preview to edit inline (`EditableInstruction` → `updateBlockInstruction`); print-with-solutions button now uses a `Key` icon. **Docs**: [UI-GUIDE.md](UI-GUIDE.md) rebuilt as the authoritative handbook (scales, control catalog, the single selected-state rule, two label tiers, "no local control styles", compliance checklist). tsc + lint green.

**2026-06-01** — Apple-HIG UI overhaul (app chrome only; worksheet print untouched). On branch `ui-overhaul`, baseline tag `ui-overhaul-baseline` (dev+main synced to it on origin). **Tokens** ([theme.css](../../src/assets/theme.css)): added 8pt spacing / radius / type / motion scales to `:root`, and per-theme layered surfaces (`--bg-base/-surface/-surface-2/-hover`), hairline `--separator`, desaturated `--accent` (was neon `#ac29e9`), `--accent-soft/-on`, `--danger`, soft `--shadow-1/2/3` + `--shadow-focus` — for all three themes; old token names kept as aliases. UI font → SF-first `--font-ui`. **index.css**: global `:focus-visible` ring, `prefers-reduced-motion`, `.ui-icon-btn`/`.ui-hover`/`.sidebar-leaf` hover-press helpers, floating scrollbar. **Controls**: `sharedPluginStyles` radio/pill → calm accent-soft segmented thumbs, sentence-case labels; `IconButton` retokened (34px, hover/press); new [Switch.tsx](../../src/components/ui/Switch.tsx) iOS toggle (used for the 3 Opdrachtinstellingen booleans). **Panels** re-skinned via their `S` objects (appStyles block-selection = accent ring not dashed; sidebar removed inline hover hacks → `.sidebar-leaf`, segmented theme switch; TopBar menu hover; Inspector cards = surface+shadow, segmented `btnGroup`, pill generate button). Killed tiny-UPPERCASE micro-labels — hierarchy now weight+size. tsc+lint green.

**2026-06-01** — Print fix + footer + natural/decimal selector. **Print**: added `print-color-adjust: exact` in [index.css](../../src/index.css) `@media print` so background-color visuals print (lijnstuk line, salmon rooster headers, fills, tints); also redrew the Breuk-van-een-lijnstuk segment line with borders instead of `background-color` so it prints regardless of the dialog's "Background graphics" toggle. **Footer** ([sidebar.tsx](../../src/components/layout/sidebar.tsx)): "Gemaakt door Ruben Van Handenhove"; removed the X link, added a Google-Form feedback link (lucide `MessageSquare`); coffee heart kept. **Natural/decimal selector** (Splitsen-Rooster style "Decimalen: Geen/1/2/3") added to **vergelijken** (getallen + kiezen) and **plaatswaarde** (waarde/plaats/tabel): generators build decimal numbers + decimal masks (t/h/d) via `getMaskPlaces`/`digitAtPlace`; PlaatswaardeViewer renders the comma + decimal places/labels (tienden/honderdsten). Even/oneven left natural-only (undefined for decimals).

**2026-05-31** — Review tweaks to the new families (build + lint green). **Romeinse**: removed the `subtractief` toggle — notation is now always subtractive (IV, curriculum standard); generator/config/defaults updated. **Scaffolding leak fix**: the fraction `subType`-keyed Differentiatie blocks in [Inspector.tsx](../../src/components/configurator/Inspector.tsx) now only fire for `typeId === 'breuken'` (scoped `subType` to breuken), so Romeinse herkennen no longer inherits the fraction scaffolding list. **Afronden** reworked: `numberType` natural/decimal (new **afronden-dec** leaf, real, rooster+simpel); decimal-safe `roundTo` + `targetsFor` (T/H/D/TD vs E/t/h); the **rooster** is now one rooster *per exercise* with a configurable "Aantal getallen per rooster" (`roosterSize`) — "aantal oefeningen" = how many roosters (laid out 2-up); decimal gets a cijfers-na-komma selector; simpel `≈`/answer lines aligned. **Sidebar**: opening a subdomain now auto-expands all its accordions at once (`openTypes` Set replaces single `openType`); individual collapse + search still work.

**2026-05-31** — Big batch: mask-menu styling fix + UI-GUIDE.md + 5 new exercise families + Geld move (build + lint green; one bracketed commit per family). **[config]** vermenigvuldigen/delen "Specifieke getalopbouw" mask buttons now use the canonical 28×28 / `var(--bg-input)` / 1px-border style + `styles.section` (was a dark hardcoded `#222226` block). **[docs]** new **[UI-GUIDE.md](UI-GUIDE.md)** documents every theme.css token (dark/light/colorblind), the canonical config/Inspector style helpers, the mask-button canon, and the sanctioned worksheet-ink hex colors; linked from CLAUDE.md ("use tokens, never hardcode bg/text/border/accent hex"). **New families** (each: typeId + generator + viewer + config + registry/UI/appstructure, reusing `getMaskPlaces`/`PLACE_VALUES`, `FragmentableGrid`, `formatMathNumber`): **plaatswaarde** (waarde/plaats/tabel), **even-oneven** (rooster kleuren / cirkels groeperen), **vergelijken** (twee getallen <,>,= / grootste-kleinste kiezen), **afronden** (rooster van afrond-doelen / eenvoudig ≈; afronden-nat now real, afronden-dec still placeholder), **romeinse-cijfers** (herkennen → getal / schrijven → Romeins, Niveau N1–N4 + subtractief toggle). **[geld]** flattened the "Rekenen met geld" accordion into direct Korting/Intrest/Winst placeholder leaves. Also: dropped the redundant subType selector from PlaatswaardeConfig (sidebar picks the view); moved "Veelvouden aanvullen" from Deelbaarheid to the "Veelvouden en delers" subdomain.

**2026-05-31** — Sidebar footer/help tweaks ([sidebar.tsx](../../src/components/layout/sidebar.tsx)). Moved the Help (?) button out of the footer to the right of the "Zoek oefening…" box (`searchWrap` now a flex row). Footer: dropped "— gratis beschikbaar", put "Gemaakt door Ruben V.H." + 𝕏 + ☕ icons on one row with "Code onder AGPL-3.0." below; set the real Buy-Me-A-Coffee link (`buymeacoffee.com/raregoudvis`, was REPLACE_ME placeholder).

**2026-05-31** — Teacher-feedback batch across breuken / geld / vermenigvuldigen / splitsen (build green). **Breuken kleuren**: "Vorm" is now 3 multi-select toggles (Rechthoek/Vierkant/Cirkel, mixed per exercise via new `shapes[]`); new `'square'` `FractionShape` renders a square in vertical strips ([FractionShapeSVG.tsx](../../src/components/viewer/FractionShapeSVG.tsx)); single-shape selection unlocks a **Vaste grootte** toggle with cm sizing under a new fractions **Geavanceerd** section (`staticSize`/`staticW`/`staticH`/`staticSide`/`staticDiam`). **Breuk van een hoeveelheid — concreet**: unified task to "Verdeel en kleur [breuk]" across all three scaffolds (dropped "van deze hoeveelheid"); new **Groepering** setting (`groupingMode`: standaard / gebalanceerd / per-deel) via `groupRows()`. **Schematisch**: draw-box width/height now adjustable in cm (`drawBoxW`/`drawBoxH`). **Abstract**: N1/N2/N3 and Berekenings-/Structuurlijnen/Blanco now show example hints under each button. **Veelhoek**: `showGrid` toggle — off = outline + colored region only, 1cm cells. **Vermenigvuldigen**: "Zonder ×1" (`excludeOne`) toggle as last engine setting, filtered in mathEngine. **Geld teruggeven**: italic "Teken dit bedrag." in the draw box, "Berekening:" in the lege-ruimte box. **Verliefde harten**: getalopbouw labels → "Getal bovenaan" / "Ingevuld getal".

**2026-05-31** — Roadmap placeholders added to `APP_STRUCTURE` (greyed, non-clickable, no generators yet) marking the next exercise types to build, based on a wiskunde-leerplan coverage analysis (L1–L6, G/`+` doelen). **Getallenkennis** (Getalbegrip): Vergelijken (<,>,=), Verbanden (breuk·decimaal·procent), Even en oneven, Plaatswaarde benoemen. **Bewerkingen**: new placeholder subdomains Hoofdrekenen (handig rekenen) [×/:10·100·1000, compenseren/splitsen, rekenvolgorde en haakjes, kettingsommen], Schattend rekenen, Controleren (negenproef). **Meten**: Tijdsduur, Kalender, Lengte en oppervlakte (liniaal/omtrek/oppervlakte), Massa (weegschaal), Maateenheden (passende maateenheid kiezen), Herleidingen (lengte/oppervlakte/inhoud/massa). New **Vraagstukken** domain (placeholder) + `--accent-vraagstukken` in all 3 themes (theme.css). Meetkunde deferred. Coverage/projection tooling lives in a local scratch dir outside the repo (not committed): generatable ceiling ≈ 60% of the leerplan, roadmap lifts coverage 30%→45% of total (50%→75% of generatable).

**2026-05-31** — Docs + release for the multi-round feature work. Updated ARCHITECTURE.md (new state slices baseSettings/curriculum/draftBlocks, history/lock-gate rules, autosave incl. curriculum, persistence v2 + lz-string + CurriculumLock, new §13 "teacher-workflow layer", refreshed file map + registry rows), CLAUDE.md (directory tree + state slices + persistence/teacher note), README (features + types table). Bumped RELEASE_VERSION → v0.5 + RELEASE_SUMMARY and package.json → 0.5.0. Pushed `dev` + `main` for external testers.

**2026-05-31** — Round-5 splitsen/temperatuur fixes (build + lint green, app boots). **Splitsen mask bug**: `generateTotal`/`generateGiven` were double-counting decimal places (PLACE_VALUES already holds t/h/d) → Rooster now honors a t-only mask. **Positie masks + decimals**: splitsbenen/plaatswaarden/positietabel get a "Specifieke getalopbouw" mask under Maximum getal; the generator now *builds* the number from that mask (numFromMask), so "only TE,t" works; positietabel gained decimals (numberToDutchWords extended with "komma" for decimal digits) and its tables are right-aligned (word fills the left, no overlap). **Soorten** pills now lay out 2×2 (Benen row / Getal row). **Manual edit**: splitsen Geavanceerd section lists a textbox per generated number; typing one re-derives that exercise (pairs / place breakdown / words) via `recomputeSplitsenExercise` + `patchExercise`. **Temperatuur**: scale capped at 25 (glass continues a bit past, unlabelled) and negatives floor at −15; "Per rij" hidden for the verschil type (one exercise per row).

**2026-05-31** — Round-4 feedback fixes (build + lint green, app boots). **Preview**: reverted the auto-fit to width-only scaling + clip (no more distorted/compressed examples). **Font weight**: swept all viewer *content* to normal weight (solutions, answers, totals, fraction digits, etc.); only the opdracht title + structural table headers stay bold. **Config restyle**: added `pill` + `onOffRow/onOffBtn` helpers to sharedPluginStyles; Splitsen "Soorten"/"Notatie" are now themed pills, Getallenas/Ordening fraction toggles are label + Aan/Uit. **Splitsen**: "Splitsen (basis)" → **Rooster**; new **Decimalen (0–3)** setting + decimal place-value masks (t/h/d) for Rooster + splitsbenen + plaatswaarden (positietabel + verliefde harten stay integer); generator does scaled-int decimal decomposition, viewer formats via formatMathNumber. **Temperatuur**: removed the duplicate Variant toggle from the engine (variant = sidebar leaf); redesigned the thermometer SVG (glass gradient, bulb, minor+major ticks); new **Verschil** type — two thermometers with per-thermometer given-info (Gekleurd/Getal/Beide) + a "verschil = ___" answer (`|c1−c2|`).

**2026-05-30** — Round-3 polish + decimal/fraction depth (build + lint green, app boots; testing build for external feedback). **Preview**: `ExercisePreview` now measures + scales AND centers the example in its white box (translate offset; inner sized to max-content). **Sidebar/engine dedup**: reverted Splitsen to separate sidebar leaves (accordion, no wiskundig) and removed the layout picker from `SplitsenConfig`; removed the duplicate "Lay-out" toggle from `DeelbaarheidConfig`; nested Klok activity (Lezen/Tekenen/Omzetten) under Analoge/Digitale klok in the sidebar and dropped the Activiteit toggle from `ClockConfig`. Catalog disambiguates multi-parent variant labels (e.g. "Analoge klok · Lezen"). **Ordening decimals**: 1/2/3 decimal places (Decimalen control + mask shows t/h/d). **Getallenas**: decimal gets fine step presets (down to 0,001) + a validated custom-step text input; rational gets Gemengde getallen + Gelijknamige (niet vereenvoudigen) toggles; geheel got a minimum-bound slider and fixed descending/negative runs (clamped start math). **Base parity**: `baseDecimalPlaces` + `baseUnitFractionsOnly`/`baseAllowMixed` in Basisinstellingen, wired via `baseApply` (ordenenDefaults declares the keys). **Bug fix**: `digitAtPlace` now scales to integers so 3-decimal place masks extract digits without float drift.

**2026-05-30** — Big batch: preview/header fixes + Splitsen/Ordenen/Getallenas upgrades (build + lint green; needs in-app verification). **Shared**: new `VerticalFraction` component (extracted from MathBlockRenderer/FractionExerciseItem, repointed both); generic `patchExercise(blockId, field, exId, patch)` store action; `numberMatchesMask` + `digitAtPlace` in mathEngine. **A — fixes**: `ExercisePreview` now measures content + scales to fit a fixed box (ResizeObserver), so mass-add cards show the WHOLE example; mass-add grid → 2 columns (minmax 460) + taller preview. Sheet header (center title): Score box moved onto the Naam/Klas row (no longer floats below). **B — Splitsen**: layout selection moved out of the sidebar (single "Splitsen" leaf) into an inspector **dropdown** (wiskundig dropped); positietabel words right-aligned so tables align; splitsbenen gained a 30-vs-3T notation + a 4-combo (benen/getal × 30/3T) include-checklist that the generator mixes; plaatswaarden notation became letters/uitgebreid checkboxes mixed per exercise (new `SplitsenExercise.notation`). **C — Ordenen**: count up to 8; click-to-edit prompt numbers (re-sorts the answer via patchExercise); "specifieke getalopbouw" place masks for natural+decimal; rational shows vertical fractions with min/max denominator + stambreuken + gemengde-getallen toggles; gehele getallen got a lower-bound slider (−maxGetal…0). **D — Getallenas**: "Beide" arrow direction (mixed per set); decimal/rational/geheel number lines now real (generator precomputes `values: (number|Fraction)[]`, viewer renders an HTML label overlay with vertical fractions + nl-BE/negative formatting).

**2026-05-30** — Inspector/sidebar/help polish (build + lint green). **Geavanceerd** sidebar group (`BaseSettingsPanel.tsx`) is now a collapsed-by-default accordion. **Opdrachtblok** card ([Inspector.tsx](../../src/components/configurator/Inspector.tsx)) redesigned to sliders: Instructie (text, placeholder "Los op."), Score (slider 0…aantal×2, clamps when aantal drops), Aantal oefeningen (slider 1–20), Witruimte (renamed Spatiëring); locked mode disables all but Aantal. **Curriculum samenstellen** left list now shows **subdomain sub-headers** (groups by `item.context`, e.g. Geld → Herkennen/Bedrag tekenen/Wissel/Teruggeven) and the modal is wider (1280 / list 360). **HelpModal** rewritten into **Voor ouders** / **Voor leerkrachten** tabs; the teacher tab has a Basis block (original sections) + a Geavanceerd block documenting Basisinstellingen and Curriculum samenstellen.

**2026-05-30** — Round-2 refinements on the three workflow features (build + lint green; needs in-app verification). **Shared `ExercisePreview`** (new `components/shared/`, replaces MassAddPreview): builds a 1-exercise throwaway block, generates directly, renders the Viewer in a fixed-height clip box (`scale(.85)`, width-compensated) so previews are uniform. **Mass-add**: now 1 example/card + a top-level **domain filter bar** (chips from new `catalogDomains()`; `CatalogItem` gained `domainId`/`domainLabel`). **Basisinstellingen** enriched + moved: `BaseSettings` now carries per-place `baseOperand1Mask`/`baseOperand2Mask` + `baseBridges` map (replacing the single `baseBridgePolicy`); `baseApply` writes them cloned+guarded (only when non-empty). New **`BaseSettingsModal`** mirrors the real NaturalSettings UI (max presets, D/H/T/E getalopbouw, GEEN/MAG/MOET bruginstellingen via `getMaskPlaces`/`getBridgePlaces`). **Curriculum samenstellen** rebuilt as a proper authoring modal (`components/curriculum/CurriculumBuilderModal.tsx`): two-pane — left = every type grouped by domain with show/hide + bulk (per-domain + global) + variant select; right = the **real config plugin** (`EXERCISE_UI[typeId].Config`) rendered against an off-sheet **`draftBlocks`** store slice (new; `updateBlockSettings` falls through to it, no history/autosave) + a live example; footer emits the locked share link from the drafts. **Top bar** decluttered: **Delen** dropdown (Blad/Sjabloon) + **⋯ Meer** overflow (Exporteer/Importeer/Presets); removed the loose share/template/lock icons and the derive-from-sheet `handleShareCurriculum`. Both **Basisinstellingen** and **Curriculum samenstellen** now live in a sidebar **Geavanceerd** group under Thema (hidden in locked parent mode).

**2026-05-30** — Three workflow features shipped (build + lint green; needs in-app click + print verification). **(1) Global base settings** — new `src/config/baseSettings.ts` (`BaseSettings` {baseMaxGetal, baseNumberType, baseBridgePolicy} + `baseApply` that maps the semantic base onto each type's own keys via `'key' in defaults` guards: maxGetal/maxRange/maxNumber, numberType, bridges E/T/H/D; breuken deliberately excluded). New collapsible `BaseSettingsPanel` at the bottom of the sidebar. Snapshotted into each new block in `addBlockFromType` (order: registry defaults → base → leaf override), so changing the base never retro-affects existing blocks. Persisted in `WorksheetFile`/autosave/presets/share. **(2) Mass-add modal** — new `src/config/exerciseCatalog.ts` (walks APP_STRUCTURE, drops placeholders/non-registry, groups leaves by typeId into variant rows) + `MassAddModal`/`MassAddPreview` (live example per type via direct `REGISTRY.generate` → `EXERCISE_UI.Viewer`, no store writes; per-item error boundary + IntersectionObserver lazy render + re-roll; variant toggles), opened from a new "Toevoegen" TopBar button; "Alles toevoegen" batch-adds then generates. **(3) Locked curriculum templates** — `WorksheetFile` v2 gains optional `curriculum: {locked, allowedTypes[{typeId,label,lockedConstraints}]}`; store `curriculum` state enforced at the single choke point (`updateBlockSettings` allow-lists only numberOfExercises+pageBreakBefore when locked; `updateBlockInstruction`/`updateBlockLayout` frozen); sidebar swaps to a flat whitelist palette (hides search + base panel), Inspector shows a lock banner + hides config/differentiation/advanced (keeps count + Genereer), new "Deel curriculum" TopBar action derives the whitelist from current blocks and shares a template link; App.tsx adds a locked-load confirm. Lock survives refresh (autosave carries curriculum).

**2026-05-30** — Five new exercise families (basic versions, refine later), all via the registry contract. **#4 ordenen** (`ordenen`, 4 numberType leaves nat/dec/rat/geh): order 3–5 numbers with `<`/`>`/both. **#2 deelbaarheid** (`deelbaarheid`): tick-table (rows × selected divisors) + veelvouden fill-row. **#3 splitsen place-value** (extends `splitsen`, now an accordion banner): new layouts `positie-tabel` (Dutch number-word → D…E digit grid, via new `numberToDutchWords` helper ≤1e6), `positie-benen` (legs → H/T/E, toggle which side blank), `positie-math` (letters `7H+9T+2E` / expanded `300+70+8`, decompose/compose), place values up to 1e9. **#6 getallenas** (`getallenas`, natural only; dec/rat/geh stay placeholders): SVG number line with arrow-direction + easy/hard blank modes, step/max/ticks knobs. **#8 temperatuur** (new "Temperatuur" subdomain under Meten en metend rekenen): SVG thermometer, variants `kleuren` (colour to a temp) / `aflezen` (read a coloured meter), negatives toggle (−20…30). Each = generator + viewer (FragmentableGrid) + config + registry/UI/appstructure rows. Build + lint green; needs in-app + print verification.

**2026-05-30** — Print footer pinned to page bottom + sticky TopBar. Footer (`<tfoot>`) was floating under the last block on a short last page; added a `height:100%` chain (html→body→#root→.print-root→.print-main→.print-area-shell→`.print-area` table) in the `@media print` block so the table fills the page, the tbody cell absorbs the slack, and `<tfoot>` sinks to the page bottom. TopBar wrapper made `position:sticky; top:0` (it's a direct child of the `overflowY:auto` center column) so the crucial toolbar stays visible while scrolling the preview. Build + lint green; needs a print check (watch for a stray blank page on near-exact-fit sheets).

**2026-05-30** — Shipped **v0.4**. Bumped `RELEASE_VERSION` → v0.4 (+ Dutch banner summary on the print improvements) and package.json → 0.4.0. Rebuilt README.md (kept the Dutch teacher intro, added an English "Architecture — how it fits together" section: 3-panel data flow, the exercise registry, store, generator contract, print system, persistence). Refreshed the now-accurate print docs in ARCHITECTURE.md §9 (+§8/§11/§12) and CLAUDE.md to the real-`<table>` repeating header/footer + `@page margin:0` + FragmentableGrid system. Committed the whole working tree (registry restructure + print rewrite + docs), fast-forwarded `main` to `dev`, tagged `v0.4`, pushed main/dev/tag to origin.

**2026-05-30** — Exercises now flow across page breaks + header field alignment. Multi-item viewers each put all items in one `display:grid`, and Chrome does not fragment a grid container across printed pages — so a block taller than the remaining space jumped wholesale to the next page (reported for "Breuk van een lijnstuk" and "analoge klok"). Added a shared `FragmentableGrid` (src/components/viewer/FragmentableGrid.tsx): a block container holding per-row grids, each row `break-inside:avoid` (`.print-row`) so rows never split but the block breaks between them — the same row-chunking pattern CijferViewer already used successfully. Converted all grid-based viewers (Clock, Fraction, MathBlockRenderer, Geld, GeldTekenen, GeldWissel, Mab, Splitsen basic+mathematic), preserving each one's columns/gaps/justify/align; flex-column/flex-wrap viewers (Cijfer, GeldTeruggeven, Splitsen verliefde-harten) already fragment and were left alone. Header: `renderFields` gained an `align` param so the fill-in fields sit opposite the title — title-left now right-aligns Naam/Klas/Nr/Datum (title-right/center unchanged). Build + lint green; needs a Chrome/Edge print check.

**2026-05-30** — Print footer/header now repeat on EVERY page (real `<table>` markup). The prior div-based `display:table-header-group`/`table-footer-group` worked in Firefox but Chrome/Edge (Blink) only repeats `<thead>`/`<tfoot>` across printed pages for **real** table elements — so the teacher's PDF showed the footer only on the last page and the repeat-header strip only on page 1. Rewrote the print card in App.tsx as a shell `<div>` (keeps the screen card look + `a4Sheet`, holds `a4Ref`, and is the positioned container for the page-break indicators, which moved out of the table) wrapping a real `<table class="print-area">` with `<thead>` (spacer + repeating name strip), `<tbody><tr><td class="print-body-cell">` (header + blocks, 16mm side padding on the cell), and `<tfoot>` (school left / vrije tekst right). Screen CSS flattens the table to block flow (display:block on table/tbody/tr/td, thead/tfoot hidden) so the preview is unchanged; print CSS uses native table groups and `.print-area-shell { display:block !important }` to override a4Sheet's inline flex (flex must never reach the `<table>` or Chrome drops `display:table` and stops repeating). Real spacer `<div>` replaces the `::before`; `.print-body-inner` dropped (the `<td>` carries padding). Build + lint green; needs a final Chrome/Edge print check.

**2026-05-30** — Robust print rewrite: real margins + repeating non-overlapping footer. Teacher's printed PDF showed `@page` margins ignored (content at paper edge), the `position:fixed` footer overlapping/cutting exercise bottoms, and a page number where vrije tekst should be — all because the layout depended on `@page` margins that the print dialog ("Margins: None/Minimum") silently zeroes. Switched to the CSS table running-header/footer technique: `.print-area` becomes `display:table` in print with `.print-thead` (table-header-group, top-margin spacer), `.print-body`/`.print-body-inner` (table-row-group, 16mm side padding), and `.print-tfoot` (table-footer-group, school-left / vrije-tekst-right). Browsers repeat header/footer groups on every page AND reserve their height, so margins and footer survive any dialog setting and content never overlaps. `@page` margin set to 0; usePrint.ts still blanks the browser margin boxes. Body wrappers are `display:contents` on screen so the preview is unchanged. Voettekst menu: dropped Paginanummering (Chrome can't count pages), moved Vrije tekst to the right zone. New "Koptekst op elke pagina herhalen" toggle (`HeaderData.repeatHeader`) extracts the name fields into a `renderFields()` helper and repeats them in `.print-thead` on every page (page-1 body copy hidden to avoid duplication). Build + lint green; needs a manual Chrome/Edge print check.

**2026-05-30** — Fix print footer + page-width regression from the print overhaul. The footer had been moved into `@page` margin boxes (`@bottom-center { content: counter(page) }`), but Chromium ignores margin-box `content`, so Chrome/Edge printed blank page bottoms. usePrint.ts (blank-boxes-only) and index.css (16mm sides, `.print-footer-bar` fixed rule, `.print-area` bottom padding) were already corrected in a prior pass; restored the missing HTML footer in App.tsx — re-added the `footerData` store selector and the print-only `.print-footer-bar` div (school | klas | leerkracht left, vrije tekst right, `space-between`). No live page number (Chrome can't count pages from HTML/CSS without a paginator). Build + lint green.

**2026-05-30** — Share-link compression + print/layout overhaul. Share: links now LZ-compressed via lz-string (`compressToEncodedURIComponent`, ~6–8× smaller) with cap raised 6000→30000; old base64 links intentionally dropped (alpha). Print/layout: new global "Ruimte tussen oefenreeksen" slider (`DocSettings.blockSpacing`); preview widened 800→920px; print side margins 18→14mm, top 15→18mm for less-cramped continuation pages; sets may now split across pages (`break-inside: avoid-page` removed) while opdracht line stays glued to its first row (`.print-opdracht break-after:avoid`) and single exercises never split; per-block "begin op nieuwe pagina" hover toggle (`MathBlock.pageBreakBefore` → `.page-break-before { break-before: page }`) with on-screen "↡ nieuwe pagina" marker; real per-page footer (centered `counter(page)` + school info) injected as `@page` margin boxes by usePrint.ts, replacing the hardcoded "Pagina 1" HTML footer. Build + lint green.

**2026-05-30** — Lint cleanup: cleared all 41 pre-existing eslint errors, no behaviour change. mathEngine dead initializers (`let d1=1,…`) → typed uninit decls; `op1: any` → `number | Fraction`, `op2` let→const; `value: any` → `unknown` in 6 config plugins; `isFraction` param `any`→`unknown`; `MathBlock.constraints` kept `any` with a documented eslint-disable; AlphaPopup lazy `useState` init (dropped the effect); eslint-disable for the boot-time autosave `setState`; eslint config override adds `argsIgnorePattern`/`varsIgnorePattern: '^_'` for intentionally-unused args. Build + lint both green.

**2026-05-30** — Central registry refactor: replaced the scattered `typeId` if-else wiring (dispatch, Inspector config mount, App viewer routing, `addBlockFromType` defaults) with a single exact-key registry — `config/exerciseRegistry.ts` (pure data: generator/field/defaultConstraints/defaultCount) + `config/exerciseUI.tsx` (Viewer/Config). Store gained a generic `setExercises(id, field, data)` action replacing the 9 per-type setters; `generateDispatch` is now a registry lookup. Added per-family constraint interfaces to types.ts. Normalized viewers to a uniform `{block, showSolutions}` (MabViewer derives mode from typeId; new ClockViewer/FractionViewer wrappers hold the grid logic moved out of App.tsx). Build + lint green. Adding a type is now: generator + viewer + config + one row in each registry file.

**2026-05-30** — Added ARCHITECTURE.md (full system map: data flow, add-a-type contract, generator contract, per-typeId registry table, print/persistence) and brought the stale CLAUDE.md back in sync (removed the dead react-pdf/WorksheetPDF section, updated dir tree + types table + add-type steps for MAB and Geld Wissel/Teruggeven).

**2026-05-27** — Print + Geld polish. Extended `@page` rule with empty `@top-*`/`@bottom-*` margin-box content so Chrome/Edge stop injecting URL/date headers and page-of-Y footers (Firefox still ignores). Tagged per-exercise wrappers in every viewer (Geld/GeldTekenen/GeldWissel/GeldTeruggeven/Mab/Splitsen/Clock + FractionExerciseItem wrapper in App.tsx) with `.print-exercise` so cells never split mid-block; `.print-block` got `break-inside: avoid-page` as soft hint. Bedrag tekenen `Voorbeelden tonen` bar shrunk (Bill 42×24, EuroCoin 26, CentCoin 22, label 9px) so all 13 denominations fit on a single line. Dropped `Hoeveelheid` (D/H/T/E) toggle from Geld herkennen — removed config section, viewer read sites, and `showPlaceValues`/`placeValues` defaults. Geld herkennen denominations defaults shrunk (Bill 56×32, EuroCoin 36, CentCoin 30 via new `width/height`/`size` props on the shared SVG components). New `Geldlayout: 'samen' | 'gescheiden'` constraint + Differentiatie toggle (herkennen only) — `gescheiden` splits bills+euro-coins onto row 1 and cent-coins onto row 2 separated by 10px gap.

**2026-05-27** — MAB v3 polish: symbolic tens rotated to horizontal bars stacked vertically (matches MAB modes, fixes wide-T-column bug). Units render in a deterministic column-first 2-row "domino" pattern (1, 2, 3, 4, 5…) for subitizing — applies to both symbolic dots and Dienes cubes. `MabStyle` renamed: `symbolic` | `mab-bw` (old `realistic`) | `mab-color` (new — units yellow, tens green, hundreds red, thousands blue; black strokes throughout). Back-compat at read sites so old saves with `'realistic'` resolve to `'mab-bw'`. Hundreds-cell bumped to 7px (flat 70×70) so the hundreds glyph reads as the biggest in MAB modes; thousands cell matches.

**2026-05-27** — MAB v2 + tekenen + sidebar branding. Scaffolding model rebuilt as `scaffolding: 'positietabel' | 'kader' | 'geen'` (was `showBox: boolean`); control moved out of MabConfig into the Differentiatie card; backwards-compat fallback at read sites. Geavanceerd accordion hoisted from MabConfig into the shared Inspector accordion (matches cijferen/geld pattern); contains Oefeningen per rij + Tekenvak hoogte + Antwoordlijn hoogte. `boxHeight` default 100→60 (slider min 40). `Tussennullen` toggle + `allowInternalZero` constraint + `hasInternalZero` helper removed — mask now sole structural control. Bug fix: `Tot N` and mask buttons clear `mabExercises` so the preview shows the empty-state prompt instead of stale out-of-range numbers. New `mab-tekenen` (Getallen tekenen) exercise: reverse of herkennen — number printed on the answer line, student draws blocks; shares generator, config, and viewer with herkennen via a `mode` prop. `Geen` scaffolding only offered for herkennen (tekenen always keeps the drawing target). Realistic glyphs: `CELL` 4→6 (60×60 hundreds flat), tens rotated to horizontal bars stacked vertically so all H/T/E columns share fixed width. Sidebar header reworked: logo left, editable site title + subtitle right (subtitle placeholder `using Basisonderwijs Vlaanderen`), both persisted to localStorage keys `enderklas_site_title_v1` / `enderklas_site_subtitle_v1`.

**2026-05-27** — MAB box redesign: drawing now splits into left-to-right H/T/E columns inside one optional box with H/T/E header labels + dividers; single always-shown answer line below. Replaced `scaffolding`/`boxBorderColor`/`useSpecifiek`/`specifiekeGetallen` constraints with `showBox` + `answerHeight`; H/T/E labels bumped to 14px centered. Per-place glyphs split out of `MabBlocksSVG.tsx`; viewer now flex-wraps glyphs per column. MabConfig adds Geavanceerd accordion (Oefeningen per rij + Tekenvak hoogte + Antwoordlijn hoogte sliders) and Met/Zonder kader toggle; Inspector cleared of MAB-specific scaffolding/exercisesPerRow. Block toolbar (lock/duplicate/trash/up/down) relocated to outside-right of the dashed border, vertically stacked. Header gained `fieldOrder` + `fieldWidths` (both optional for back-compat); App.tsx renders fields via `fieldOrder` loop with per-field width, Inspector replaces 4-checkbox grid with reorderable rows (hover ↑/↓ buttons) + per-field length sliders. Centered title position with no title now positions score in flow (`justify-content: flex-end`) so it can't overlap the header answer lines.

**2026-05-27** — Teacher-quality-of-life batch (eight items): (1) Auto-save to `enderklas_autosave_v1` localStorage key, debounced 1.5s via module-level zustand subscribe; boot-time banner offers restore when current sheet is empty. (2) Named preset library (`enderklas_presets_v1`, max 20) with new `PresetModal` (save current + list with Laad/× per row). (3) `document.title` follows `headerData.titel`. (4) Sidebar search filters APP_STRUCTURE tree by label and auto-expands matches. (5) `duplicateBlock` store action + 🗐 icon in block-control overlay (deep-clones constraints + exercises, resets `locked`, switches activeBlock to clone). (6) Donate ❤ link in sidebar footer pointing at Buy Me a Coffee (placeholder URL `buymeacoffee.com/REPLACE_ME` — TODO replace before merge). (7) Release update-banner driven by `RELEASE_VERSION`/`RELEASE_SUMMARY` in new `src/config/version.ts`; dismissed-version cached in `enderklas_release_seen_v1`. (8) Werkbundel via deelbare URL — base64 in `#share=…` hash (not query, so it doesn't leak through server logs); 🔗 Deel button copies link, 6 KB cap with fallback alert, boot-time hash detection with confirm-replace. HelpModal expanded to cover all new features.

**2026-05-27** — Document-wide feature batch: (1) `Genereer alles` in TopBar regenerates all non-locked blocks; new shared `services/generateDispatch.ts` is now single source of truth for typeId→generator routing (Inspector uses it too). (2) Per-block 🔒 lock toggle in block-control overlay + `locked` flag on `MathBlock`; locked blocks skipped by bulk regen. (3) JSON Exporteer/Importeer (`services/persistence.ts`) — versioned schema (v1) covering blocks/header/footer/docSettings; import confirms-and-replaces. (4) Doc-wide `numberBlocks` toggle in Opdrachtinstellingen renders `1.`/`2.`/… prefix on block headers (render-time, doesn't mutate instructionText). (5) Theme system moved into store with three themes (dark/light/colorblind); ☀/☽ sidebar toggle replaced by 3-way Thema card in Inspector; `[data-theme="colorblind"]` block in theme.css uses IBM CB-safe palette. (6) Contact (X-link) + Help icon buttons added to sidebar footer; new `HelpModal` component with sectional Dutch usage guide (cloned from AlphaPopup pattern, follows theme variables).

**2026-05-27** — Added MAB > Getallen herkennen exercise type under Getallenkennis > Getalbegrip. New `MabExercise` type + `mabExercises` field on `MathBlock`. Generator at `services/mab/mabGenerator.ts` supports random-with-mask and manual-number-list paths, optional internal-zero suppression. Viewer renders place-value blocks via new `MabBlocksSVG` component with two styles: 'symbolic' (dot/bar/square) and 'realistic' (Dienes 1×1/1×10 rod/10×10 flat/10×10×10 cube). Scaffolding: answer 'lijn' (dotted line) or 'tabel' (H/T/E columns with row). Config plugin exposes style, maxNumber (10/20/100/1000), mask, manual list, internal-zero toggle, box border color, box height. Sidebar entry added as MAB accordion with 'Getallen herkennen' leaf + 'Getallen tekenen' placeholder.

**2026-05-27** — Print hid worksheet behind mobile-block overlay because print preview narrows the viewport and re-triggers the `max-width: 768px` media query; added `.mobile-block { display: none !important; }` inside `@media print`.

**2026-05-27** — Multiple cijferen mask fixes: Getal 1 multiplicand mask fell back to random when applyMask failed range check (hasMask0 guard added); Getal 2 multiplier/divisor masks ignored decimal keys (t/h) because applyMask was called with dp=0 — fixed to pass dp so fractional multipliers/divisors work; header display for fractional operands now uses dp instead of rounding to integer; AlphaPopup wired into App.tsx; mobile/tablet block added; @page margin-top:15mm for consistent top margin on new pages; print-block no longer has break-inside:avoid (blocks can split, individual exercises cannot); partial products in multiplication hidden unless showSolutions; answer rows in add/sub and multiplication moved directly below thick line (removed gap row).

**2026-05-27** — Multiple fixes: header writing lines expand to full width; print produces multiple pages (root cause: `overflow: hidden` + `height: 100vh` on html/body/#root overridden in print CSS); cijferen division quotient columns now always match dividend digit count; multiplication Getal 2 mask was overridden by `minMultiplier` clamp — fixed so masked multiplier only clamps to ≥2; print exercise page-break-inside avoidance added at block and exercise level.

**2026-05-26** — Teruggeven config polish: diagram font/size tuned, middle node shows € + blank line in ingevuld mode, Centpatroon updated to Belgian-only options (25ct/10ct/5ct), added "Leeg" scaffolding level (no diagram, just answer field), Prijsbereik moved to preset buttons with auto-updating betalen-met options.

**2026-05-26** — Added Geld > Wissel (bill + drawing box) and Geld > Teruggeven (change-giving arrow diagram with 5 scaffolding levels, 2 answer types) as new active exercise types; renamed "Tekenen" to "Bedrag tekenen" and removed "Gepast betalen" placeholder.

**2026-05-26** — Rewrote CLAUDE.md with full codebase documentation (directory tree, all 8 exercise types, data flow, step-by-step guide for adding new exercise types, code commenting guidelines for junior devs). Created UpdateState.md and added Stop hook to auto-remind about session summaries.
